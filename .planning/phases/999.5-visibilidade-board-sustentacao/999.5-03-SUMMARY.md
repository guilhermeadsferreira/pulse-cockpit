---
phase: 999.5-visibilidade-board-sustentacao
plan: "03"
subsystem: settings-ui
tags: [settings, jira, sustentacao, ui]
dependency_graph:
  requires:
    - AppSettings.jiraSupportBoardId (999.5-01)
    - AppSettings.jiraSupportProjectKey (999.5-01)
    - AppSettings.jiraSlaThresholds (999.5-01)
  provides:
    - UI de configuração do board de sustentação (SettingsView.tsx)
  affects:
    - Planos subsequentes que dependem do usuário configurar o board de sustentação
tech_stack:
  added: []
  patterns:
    - Estado local (string) para edição de JSON — onBlur faz parse e atualiza settings
    - Erro inline para validação de campo sem submit
key_files:
  created: []
  modified:
    - src/renderer/src/views/SettingsView.tsx
decisions:
  - slaThresholdsText como estado local string inicializado via useEffect — evita re-render a cada keystroke no JSON
  - onBlur (não onChange) para parse do JSON — evita erros de parse durante digitação intermediária
  - Subseção inline no JSX da seção Jira — sem novo componente, seguindo diretriz do plano
metrics:
  duration: 60s
  completed: "2026-04-02"
  tasks_completed: 1
  tasks_total: 1
  files_modified: 1
---

# Phase 999.5 Plan 03: UI Configuração Board de Sustentação Summary

Seção "Board de Sustentação" adicionada inline na SettingsView dentro da seção Jira, com campos jiraSupportProjectKey (texto), jiraSupportBoardId (número) e jiraSlaThresholds (textarea JSON com parse no onBlur e erro inline).

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Adicionar campos do board de sustentação na SettingsView | 70f90bd | src/renderer/src/views/SettingsView.tsx |

## What Was Built

- **Campo jiraSupportProjectKey** — input texto com placeholder "ex: SUP, HELP, ATC", mapeado diretamente para `form.jiraSupportProjectKey`
- **Campo jiraSupportBoardId** — input number com `Number(e.target.value) || undefined`, mapeado para `form.jiraSupportBoardId`
- **Campo jiraSlaThresholds** — textarea JSON (rows=3, font-family monospace), estado local `slaThresholdsText`, parse no `onBlur`, erro inline "JSON inválido" quando parse falha
- **Separador visual** — `borderTop: '1px solid var(--border-subtle)'` delimita a subseção dos campos Jira principais
- **Heading da subseção** — `<div>Board de Sustentação</div>` com fontWeight 500
- **Inicialização via useEffect** — `setSlaThresholdsText(JSON.stringify(s.jiraSlaThresholds ?? {}, null, 2))` quando settings carregam

## Verification

- `grep -c "jiraSupportProjectKey|jiraSupportBoardId|jiraSlaThresholds" SettingsView.tsx` — retorna 6 (>= 3 requerido)
- `grep -n "Board de Sustentação" SettingsView.tsx` — confirma seção dedicada nas linhas 486, 488, 491, 503
- Novos campos estão após linha 480 (após `jiraBoardId`) — ordem correta
- `npx tsc --noEmit` — zero erros

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None — os campos leem e gravam em `form.jiraSupportProjectKey`, `form.jiraSupportBoardId` e `form.jiraSlaThresholds`, que são persistidos via `window.api.settings.save(form)` pelo mecanismo existente de save.

## Self-Check: PASSED

- src/renderer/src/views/SettingsView.tsx — FOUND (modified, contains jiraSupportProjectKey, jiraSupportBoardId, jiraSlaThresholds, Board de Sustentação)
- Commit 70f90bd — FOUND
