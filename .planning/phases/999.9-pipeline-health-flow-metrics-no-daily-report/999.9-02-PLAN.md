---
phase: 999.9-pipeline-health-flow-metrics-no-daily-report
plan: 02
type: execute
wave: 2
depends_on: ["999.9-01"]
files_modified:
  - src/main/external/DailyReportGenerator.ts
  - src/main/prompts/daily-analysis.prompt.ts
autonomous: true
requirements:
  - PLHF-05
  - PLHF-06
  - PLHF-07

must_haves:
  truths:
    - "Secao 'O que fez ontem' mostra commits agrupados por repo com contagem em vez de listing individual"
    - "PRs merged e reviews continuam como itens individuais (nao compactados)"
    - "Review comments substantivos (> 20 chars) sao mantidos no report"
    - "Haiku analysis recebe dados de Pipeline Health como input adicional e pode gerar insights sobre gargalos"
  artifacts:
    - path: "src/main/external/DailyReportGenerator.ts"
      provides: "Commits compactados por repo + pipelineHealth no analysisInput"
      contains: "commits em"
    - path: "src/main/prompts/daily-analysis.prompt.ts"
      provides: "Prompt atualizado com secao Pipeline Health e instrucao para analisar gargalos"
      contains: "pipelineHealth"
  key_links:
    - from: "buildReport"
      to: "analysisInput.pipelineHealth"
      via: "campo pipelineHealth adicionado ao objeto analysisInput"
      pattern: "pipelineHealth"
    - from: "buildDailyAnalysisPrompt"
      to: "DailyAnalysisInput.pipelineHealth"
      via: "prompt usa dados de pipeline para gerar insights"
      pattern: "Pipeline Health"
---

<objective>
Compactar secao "O que fez ontem" (commits agrupados por repo) e enriquecer a analise de IA (Haiku) com dados de Pipeline Health para que gere insights sobre gargalos do pipeline.

Purpose: Reduzir ruido no report (commits individuais sao baixo valor para o gestor) e permitir que a IA identifique padroes de gargalo no fluxo.
Output: DailyReportGenerator.ts com commits compactados + daily-analysis.prompt.ts enriquecido.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.9-pipeline-health-flow-metrics-no-daily-report/999.9-CONTEXT.md
@.planning/phases/999.9-pipeline-health-flow-metrics-no-daily-report/999.9-01-SUMMARY.md
@src/main/external/DailyReportGenerator.ts
@src/main/prompts/daily-analysis.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compactar commits na secao "O que fez ontem"</name>
  <files>src/main/external/DailyReportGenerator.ts</files>
  <read_first>
    - src/main/external/DailyReportGenerator.ts (secao "O que fez ontem" no buildReport — buscar por "O que fez ontem" e o bloco de githubCommits logo abaixo, linhas ~773-846)
  </read_first>
  <action>
**Substituir o bloco de commits individuais (per D-12):**

Localizar no buildReport a secao que itera sobre `activity.githubCommits` (atualmente faz listing individual com emoji de commit por commit).

Substituir de:
```typescript
if (activity.githubCommits.length > 0) {
  for (const commit of activity.githubCommits) {
    const msg = commit.message.length > 120 ? commit.message.slice(0, 117) + '...' : commit.message
    lines.push(`- 💻 \`${commit.repo}\`: ${msg}`)
    hasActivity = true
  }
}
```

Para (per D-12 — contagem resumida por repo):
```typescript
if (activity.githubCommits.length > 0) {
  // Agrupar commits por repo (per D-12: contagem resumida em vez de listing individual)
  const commitsByRepo = new Map<string, number>()
  for (const commit of activity.githubCommits) {
    commitsByRepo.set(commit.repo, (commitsByRepo.get(commit.repo) ?? 0) + 1)
  }
  for (const [repo, count] of commitsByRepo) {
    lines.push(`- 💻 ${count} commit${count > 1 ? 's' : ''} em \`${repo}\``)
  }
  hasActivity = true
}
```

**Manter PRs merged e reviews como itens individuais (per D-13):**
Nao alterar os blocos de `githubPRsMerged` e `githubReviews` — permanecem com listing individual.

**Manter review comments substantivos (per D-14):**
Nao alterar o bloco de review comments que ja filtra por `c.body.trim().length > 20`. O filtro ja esta correto.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && grep -c "commits em" src/main/external/DailyReportGenerator.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "commitsByRepo" src/main/external/DailyReportGenerator.ts returns matches (Map agrupamento)
    - grep "commits em" src/main/external/DailyReportGenerator.ts returns match (contagem por repo)
    - grep "commit.message.length > 120" src/main/external/DailyReportGenerator.ts returns NO match (listing individual removido)
    - grep "PR merged" src/main/external/DailyReportGenerator.ts returns matches (mantido individual per D-13)
    - grep "Review em" src/main/external/DailyReportGenerator.ts returns match (reviews mantidos individual per D-13)
    - TypeScript compila sem erros
  </acceptance_criteria>
  <done>
    - Commits aparecem como "3 commits em banking.transactional-schedule-api" em vez de listing por commit
    - PRs merged e reviews mantidos como itens individuais
    - Review comments substantivos (> 20 chars) mantidos
  </done>
</task>

<task type="auto">
  <name>Task 2: Enriquecer Haiku analysis com dados de Pipeline Health</name>
  <files>src/main/external/DailyReportGenerator.ts, src/main/prompts/daily-analysis.prompt.ts</files>
  <read_first>
    - src/main/external/DailyReportGenerator.ts (buildReport — secao de analysisInput no final, linhas ~947-1011; runHaikuAnalysis linhas ~610-672)
    - src/main/prompts/daily-analysis.prompt.ts (DailyAnalysisInput interface linha ~6, buildDailyAnalysisPrompt linha ~12)
  </read_first>
  <action>
**1. Adicionar campo pipelineHealth ao analysisInput (per D-15):**

No `buildReport`, onde o `analysisInput` e construido (final do metodo), adicionar campo `pipelineHealth`:

```typescript
// Construir texto de pipeline health para analise IA
let pipelineHealthText = ''
if (allActiveTasks && allActiveTasks.length > 0) {
  const pipelineData = computePipelineHealth(allActiveTasks, doneTasksPhaseTime ?? new Map())
  if (pipelineData.length > 0) {
    const phLines: string[] = []
    for (const phase of pipelineData) {
      const tempoStr = phase.tempoMedio !== null ? `${phase.tempoMedio.toFixed(1)}d` : '—'
      const baselineStr = phase.baseline !== null ? `${phase.baseline.toFixed(1)}d` : '—'
      const statusLabel = phase.status === 'ok' ? 'ok' : phase.status === 'warning' ? 'atencao' : 'critico'
      phLines.push(`${phase.fase}: ${phase.tasks} tasks, tempo medio ${tempoStr}, baseline ${baselineStr}, status ${statusLabel}`)
      if (phase.maisAntiga) {
        phLines.push(`  Mais antiga: ${phase.maisAntiga.key} (${phase.maisAntiga.dias}d)`)
      }
    }
    pipelineHealthText = phLines.join('\n')
  }
}

const analysisInput = {
  sprintOverview: sprintSummaryText,
  perPersonSummary: perPersonLines.join('\n\n'),
  alerts: alerts.join('\n'),
  pipelineHealth: pipelineHealthText,
}
```

Nota: `computePipelineHealth` ja foi criada no Plan 01 e esta disponivel. Se o Plan 01 armazenou `pipelineData` como variavel local acima no buildReport, reutilizar em vez de recomputar. Caso contrario, chamar novamente (funcao pura, sem side effects).

**2. Atualizar tipo do analysisInput em `runHaikuAnalysis`:**

Atualizar a assinatura de `runHaikuAnalysis` para aceitar o novo campo:
```typescript
private async runHaikuAnalysis(
  settings: AppSettings,
  analysisInput: { sprintOverview: string; perPersonSummary: string; alerts: string; pipelineHealth: string },
): Promise<string | null>
```

**3. Atualizar `DailyAnalysisInput` no prompt:**

Em `src/main/prompts/daily-analysis.prompt.ts`, adicionar campo ao interface:
```typescript
interface DailyAnalysisInput {
  sprintOverview: string
  perPersonSummary: string
  alerts: string
  pipelineHealth: string
}
```

**4. Atualizar `buildDailyAnalysisPrompt` para incluir Pipeline Health:**

Adicionar nova secao no prompt, APOS "### Alertas ja identificados" e ANTES de "## Instrucoes":
```typescript
### Pipeline Health (fluxo por fase)
${input.pipelineHealth || 'Sem dados de pipeline.'}
```

**5. Atualizar instrucoes do prompt para Haiku:**

Na secao "## Instrucoes", adicionar item 5 (apos os 4 existentes):
```
5. **Gargalos de pipeline** — ex: muitas tasks em Code Review sugere falta de reviewers, Ready to Deploy acumulando sugere janela de deploy restrita, tempo medio em Dev muito acima do baseline sugere tasks subdimensionadas
```

Na secao "## Regras", adicionar:
```
- Quando Pipeline Health mostrar uma fase com status critico, SEMPRE mencionar na primeira observacao
```
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "pipelineHealth" src/main/external/DailyReportGenerator.ts returns >= 3 matches (field in analysisInput + construction + runHaikuAnalysis signature)
    - grep "pipelineHealth" src/main/prompts/daily-analysis.prompt.ts returns >= 2 matches (interface field + usage in prompt)
    - grep "Pipeline Health" src/main/prompts/daily-analysis.prompt.ts returns match (section header in prompt)
    - grep "Gargalos de pipeline" src/main/prompts/daily-analysis.prompt.ts returns match (instrucao 5)
    - grep "status critico" src/main/prompts/daily-analysis.prompt.ts returns match (regra adicional)
    - TypeScript compila sem erros
  </acceptance_criteria>
  <done>
    - analysisInput contem campo pipelineHealth com dados textuais de cada fase
    - DailyAnalysisInput no prompt inclui campo pipelineHealth
    - Prompt do Haiku contem secao "Pipeline Health" com dados de fluxo
    - Instrucoes do prompt orientam Haiku a gerar insights sobre gargalos de pipeline
    - TypeScript compila limpo
  </done>
</task>

</tasks>

<verification>
- TypeScript compila sem erros: `npx tsc --noEmit`
- grep confirma commits compactados por repo
- grep confirma pipelineHealth no analysisInput e no prompt
</verification>

<success_criteria>
- Secao "O que fez ontem" mostra commits agrupados por repo (contagem) em vez de listing individual
- PRs e reviews mantidos individuais
- Haiku analysis recebe dados de Pipeline Health e pode gerar insights sobre gargalos
- Prompt inclui instrucoes explicitas para analise de pipeline
- TypeScript compila limpo
</success_criteria>

<output>
After completion, create `.planning/phases/999.9-pipeline-health-flow-metrics-no-daily-report/999.9-02-SUMMARY.md`
</output>
