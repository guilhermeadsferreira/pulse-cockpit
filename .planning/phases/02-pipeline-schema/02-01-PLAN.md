---
phase: 02-pipeline-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/ingestion/ArtifactWriter.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "Temas semanticamente equivalentes (substring match apos normalizacao) sao mesclados — o mais especifico sobrevive"
    - "Health history nunca ultrapassa 50 entradas ativas — entradas antigas sao comprimidas em resumos mensais"
  artifacts:
    - path: "src/main/ingestion/ArtifactWriter.ts"
      provides: "Fuzzy theme dedup + health history cleanup"
      contains: "normalizeForComparison"
  key_links:
    - from: "ArtifactWriter.writeCeremonySinal"
      to: "normalizeForComparison"
      via: "fuzzy merge replaces new Set()"
      pattern: "deduplicateThemes"
    - from: "ArtifactWriter.writeProfile"
      to: "normalizeForComparison"
      via: "fuzzy merge on temas_atualizados"
      pattern: "deduplicateThemes"
    - from: "ArtifactWriter.appendToBlock saude_historico"
      to: "compressHealthHistory"
      via: "called after append when entries > 50"
      pattern: "compressHealthHistory"
---

<objective>
Corrigir deduplicacao de temas recorrentes e adicionar cleanup automatico do health history no ArtifactWriter.

Purpose: Temas duplicados (ex: "comunicacao" e "comunicacao assertiva") poluem o perfil. Health history cresce indefinidamente, degradando qualidade da compressao e prompts.
Output: ArtifactWriter.ts com fuzzy theme dedup e health history auto-compression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-schema/02-CONTEXT.md

@src/main/ingestion/ArtifactWriter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fuzzy theme deduplication (PIPE-01)</name>
  <files>src/main/ingestion/ArtifactWriter.ts</files>
  <read_first>
    - src/main/ingestion/ArtifactWriter.ts (full file — especially lines 647-657 for writeCeremonySinal temas merge, lines 287-289 for writeProfile temas, and the SECTION constant at line 9)
  </read_first>
  <action>
Add a private helper function `normalizeForComparison(tema: string): string` to the ArtifactWriter class that:
1. Converts to lowercase
2. Removes accents via `str.normalize('NFD').replace(/[\u0300-\u036f]/g, '')`
3. Trims whitespace

Add a private method `deduplicateThemes(themes: string[]): string[]` that:
1. Iterates over the array, for each theme checks against already-accepted themes using `normalizeForComparison`
2. Substring check: if normalizedA includes normalizedB or normalizedB includes normalizedA, keep only the LONGER original string (per D-01, D-02 — more specific survives)
3. Returns the deduplicated array preserving original labels with accents (per D-04)

Replace the dedup logic at line 655 in `writeCeremonySinal`:
- Current: `const merged = [...new Set([...currentTemas, ...newTemas])]`
- New: `const merged = this.deduplicateThemes([...currentTemas, ...newTemas])`

Also apply dedup at line 288-289 in `writeProfile` (the `updateProfile` method):
- Current: `const temasLines = result.temas_atualizados.map((t) => '- ' + t).join('\n')`
- New: `const dedupedTemas = this.deduplicateThemes(result.temas_atualizados)` then `const temasLines = dedupedTemas.map((t) => '- ' + t).join('\n')`

Also apply dedup in the `buildNewProfile` method (around line 171) where temas_atualizados is used for brand new profiles:
- After: `const temasLines = result.temas_atualizados.map((t) => '- ' + t).join('\n') || ''`
- New: `const dedupedTemas = this.deduplicateThemes(result.temas_atualizados)` then `const temasLines = dedupedTemas.map((t) => '- ' + t).join('\n') || ''`
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ArtifactWriter.ts contains `private normalizeForComparison(tema: string): string`
    - ArtifactWriter.ts contains `private deduplicateThemes(themes: string[]): string[]`
    - ArtifactWriter.ts contains `normalize('NFD')` and `replace(/[\u0300-\u036f]/g, '')`
    - Line with `new Set([...currentTemas, ...newTemas])` no longer exists — replaced by `this.deduplicateThemes`
    - The `updateProfile` method calls `this.deduplicateThemes(result.temas_atualizados)`
    - The `buildNewProfile` method calls `this.deduplicateThemes(result.temas_atualizados)`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Temas "comunicacao" e "comunicacao assertiva" merge into "comunicacao assertiva" (most specific). All 3 temas write-points use fuzzy dedup.</done>
</task>

<task type="auto">
  <name>Task 2: Health history auto-compression (PIPE-02)</name>
  <files>src/main/ingestion/ArtifactWriter.ts</files>
  <read_first>
    - src/main/ingestion/ArtifactWriter.ts (lines 295-301 for writeProfile saude append, lines 660-667 for writeCeremonySinal saude append, and lines 806-815 for extractBlock helper)
  </read_first>
  <action>
Add a private method `compressHealthHistory(content: string): string` to ArtifactWriter that:

1. Extracts the saude_historico block via `this.extractBlock(content, 'saude_historico')`
2. Parses each line matching pattern `- YYYY-MM-DD | indicador | motivo` into objects `{ date, month (YYYY-MM), indicador, motivo }`
3. Counts total active (non-compressed) entries. A compressed entry matches pattern `- YYYY-MM:` (monthly summary format)
4. If total active entries <= 50, return content unchanged
5. If > 50: group the OLDEST entries (beyond the 50 most recent) by month (YYYY-MM)
6. For each month group, generate a summary line: `- {YYYY-MM}: {N}x {indicador counts} ({motivo mais frequente})` — example: `- 2026-01: 4x verde, 2x amarelo (sobrecarga)`
7. Replace the saude_historico block with: compressed summaries (oldest first) + the 50 most recent active entries
8. Use `this.replaceBlock(content, 'saude_historico', newBody)` to write back

Call `compressHealthHistory` in two places:
- In `updateProfile`, after the saude append block (after line 301): `updated = this.compressHealthHistory(updated)`
- In `writeCeremonySinal`, after the saude append block (after line 667): `updated = this.compressHealthHistory(updated)`

Per D-05, D-06, D-07: trigger is on ingestion after append, strategy is monthly summary with counts + most frequent motivo, threshold is 50 active entries.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ArtifactWriter.ts contains `private compressHealthHistory(content: string): string`
    - Method parses lines matching `- YYYY-MM-DD |` as active entries
    - Method recognizes `- YYYY-MM:` lines as already-compressed (skips them)
    - Threshold check: `if (activeEntries.length <= 50)` returns unchanged
    - Compressed format includes count per indicador and motivo mais frequente
    - Called after saude append in both `updateProfile` and `writeCeremonySinal`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Health history auto-compresses oldest entries into monthly summaries when exceeding 50 active entries. Trend is preserved via counts; detail is preserved for recent 50.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- ArtifactWriter.ts contains both `deduplicateThemes` and `compressHealthHistory` methods
- No `new Set([...currentTemas` pattern remains in file
- Both saude_historico append points are followed by `compressHealthHistory` call
</verification>

<success_criteria>
1. Theme dedup uses normalized substring matching — "comunicacao" + "comunicacao assertiva" = "comunicacao assertiva"
2. Health history stays under 50 active entries with monthly compression of older data
3. TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-schema/02-01-SUMMARY.md`
</output>
