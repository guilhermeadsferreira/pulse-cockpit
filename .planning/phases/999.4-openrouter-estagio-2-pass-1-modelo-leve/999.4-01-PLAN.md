---
phase: 999.4-openrouter-estagio-2-pass-1-modelo-leve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/ingestion/ClaudeRunner.ts
  - src/main/ingestion/IngestionPipeline.ts
autonomous: true
requirements:
  - PERF-01

must_haves:
  truths:
    - "Com useHybridModel=true e openRouterApiKey preenchida, Pass 1 chama OpenRouter em vez de Claude CLI"
    - "Se OpenRouter retornar erro HTTP ou network error, Pass 1 executa fallback automático para Claude CLI com log de warning"
    - "Se OpenRouter retornar JSON com schema inválido (validateIngestionResult falhar), Pass 1 executa fallback para Claude CLI"
    - "Com useHybridModel=false ou sem openRouterApiKey, Pass 1 continua usando Claude CLI sem nenhuma mudança de comportamento"
    - "O payload OpenRouter para Pass 1 inclui campo system com instrução de output JSON puro"
  artifacts:
    - path: "src/main/ingestion/ClaudeRunner.ts"
      provides: "runOpenRouterPrompt com parâmetro opcional systemPrompt"
      exports: ["runOpenRouterPrompt", "runClaudePrompt", "ClaudeRunnerResult"]
    - path: "src/main/ingestion/IngestionPipeline.ts"
      provides: "Rota híbrida no Pass 1 dentro de processItem"
      contains: "if (hybridActive)"
  key_links:
    - from: "src/main/ingestion/IngestionPipeline.ts (processItem)"
      to: "src/main/ingestion/ClaudeRunner.ts (runOpenRouterPrompt)"
      via: "chamada condicional com hybridActive"
      pattern: "runOpenRouterPrompt.*promptPass1"
    - from: "src/main/ingestion/IngestionPipeline.ts (processItem)"
      to: "src/main/ingestion/SchemaValidator.ts (validateIngestionResult)"
      via: "validação pós-OpenRouter que aciona fallback"
      pattern: "validation1\\.valid"
---

<objective>
Migrar o Pass 1 do pipeline de ingestão para OpenRouter quando o modelo híbrido estiver ativo, replicando o padrão de roteamento condicional com fallback já estabelecido pelo Pass Cerimônia na fase 999.3.

Purpose: Pass 1 roda em todo artefato ingestionado (sem exceções), tornando-o o maior multiplicador de latência do pipeline. Ao roteá-lo para um modelo leve via OpenRouter quando o híbrido estiver ativo, o gestor observa redução de latência proporcional ao volume de artefatos ingestionados.

Output: `ClaudeRunner.ts` com suporte a `systemPrompt` opcional no payload OpenRouter; `IngestionPipeline.ts` com roteamento híbrido no Pass 1 idêntico ao padrão do Pass Cerimônia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.4-openrouter-estagio-2-pass-1-modelo-leve/999.4-CONTEXT.md
@.planning/STATE.md

<interfaces>
<!-- Interfaces existentes relevantes para o executor. Extraídas do codebase. -->

De src/main/ingestion/ClaudeRunner.ts:
```typescript
export interface ClaudeRunnerResult {
  success: boolean
  data?: unknown
  rawOutput?: string
  error?: string
}

// Assinatura ATUAL (a ser estendida na Task 1):
export async function runOpenRouterPrompt(
  apiKey: string,
  model: string,
  prompt: string,
  timeoutMs = 60_000,
): Promise<ClaudeRunnerResult>

// Payload atual enviado ao OpenRouter (sem campo system):
body: JSON.stringify({
  model,
  messages: [{ role: 'user', content: prompt }],
  temperature: 0,
})
```

De src/main/ingestion/IngestionPipeline.ts — padrão do Pass Cerimônia (~linha 291) a replicar:
```typescript
const settings = SettingsManager.load()
const hybridActive = !!(settings.useHybridModel && settings.openRouterApiKey)
const openRouterModel = settings.openRouterModel ?? 'google/gemma-3-27b-it'

// ... dentro do loop ...
if (hybridActive) {
  result = await runOpenRouterPrompt(settings.openRouterApiKey!, openRouterModel, prompt, 60_000)
  if (!result.success) {
    console.warn(`[IngestionPipeline] OpenRouter fallback para "${slug}": ${result.error}`)
    result = await runClaudePrompt(claudeBinPath, prompt, 60_000)
  }
} else {
  result = await runClaudePrompt(claudeBinPath, prompt, 60_000)
}
```

De src/main/ingestion/IngestionPipeline.ts — Pass 1 atual (~linha 946) a substituir:
```typescript
// Pass 1: identify pessoa_principal (no perfil context yet)
const promptPass1 = buildIngestionPrompt({
  teamRegistry,
  perfilMdRaw: null,
  artifactContent: text,
  today,
  managerName,
})
const resultPass1 = await runClaudePrompt(settings.claudeBinPath, promptPass1, 90_000)
if (!resultPass1.success || !resultPass1.data) {
  throw new Error(resultPass1.error || 'Claude não retornou dados válidos')
}

const validation1 = validateIngestionResult(resultPass1.data)
if (!validation1.valid) {
  const details = [...validation1.missingFields.map(f => `campo ausente: ${f}`), ...validation1.typeErrors].join('; ')
  throw new Error(`Schema inválido na saída do Claude (pass 1): ${details}`)
}
```

De src/main/registry/SettingsManager.ts — campos disponíveis em AppSettings:
```typescript
useHybridModel: boolean
openRouterApiKey: string | undefined
openRouterModel: string | undefined  // default: 'google/gemma-3-27b-it'
claudeBinPath: string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender runOpenRouterPrompt com suporte a system prompt</name>
  <files>src/main/ingestion/ClaudeRunner.ts</files>

  <read_first>
    - src/main/ingestion/ClaudeRunner.ts (arquivo completo — ver assinatura atual de runOpenRouterPrompt e o body do fetch)
  </read_first>

  <action>
Adicionar parâmetro opcional `systemPrompt?: string` à função `runOpenRouterPrompt` em `ClaudeRunner.ts`.

**Assinatura nova:**
```typescript
export async function runOpenRouterPrompt(
  apiKey: string,
  model: string,
  prompt: string,
  timeoutMs = 60_000,
  systemPrompt?: string,
): Promise<ClaudeRunnerResult>
```

**Mudança no payload do fetch** — adicionar mensagem de sistema quando `systemPrompt` estiver presente:
```typescript
body: JSON.stringify({
  model,
  messages: [
    ...(systemPrompt ? [{ role: 'system', content: systemPrompt }] : []),
    { role: 'user', content: prompt },
  ],
  temperature: 0,
}),
```

Nenhuma outra mudança no arquivo. O campo é opcional: chamadas existentes (Pass Cerimônia) continuam funcionando sem alteração — `systemPrompt` undefined não adiciona a mensagem de sistema.
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - ClaudeRunner.ts contém `systemPrompt?: string` na assinatura de `runOpenRouterPrompt`
    - ClaudeRunner.ts contém `...(systemPrompt ? [{ role: 'system', content: systemPrompt }] : [])` no body do fetch
    - `npx tsc --noEmit` sai sem erros de tipo relacionados ao ClaudeRunner
    - Chamada existente no Pass Cerimônia (`runOpenRouterPrompt(settings.openRouterApiKey!, openRouterModel, prompt, 60_000)`) continua válida sem modificação (parâmetro opcional)
  </acceptance_criteria>

  <done>runOpenRouterPrompt aceita systemPrompt opcional; sem mudança de comportamento para callers existentes</done>
</task>

<task type="auto">
  <name>Task 2: Rota híbrida no Pass 1 do IngestionPipeline</name>
  <files>src/main/ingestion/IngestionPipeline.ts</files>

  <read_first>
    - src/main/ingestion/IngestionPipeline.ts (ler linhas 940–970 para ver o bloco completo do Pass 1 a substituir)
    - src/main/ingestion/IngestionPipeline.ts (ler linhas 280–340 para ver o padrão do Pass Cerimônia a replicar)
    - src/main/ingestion/ClaudeRunner.ts (confirmar nova assinatura após Task 1)
  </read_first>

  <action>
Substituir o bloco do Pass 1 em `processItem` (~linha 946) pelo padrão de roteamento híbrido.

**Passo 1 — Ler hybridActive e openRouterModel** logo antes do bloco do Pass 1 (após a linha `const managerName = ...`):
```typescript
const hybridActive = !!(settings.useHybridModel && settings.openRouterApiKey)
const openRouterModel = settings.openRouterModel ?? 'google/gemma-3-27b-it'
const PASS1_SYSTEM_PROMPT = 'You must respond with valid JSON only. Do not include markdown code blocks, explanations, or any text outside the JSON object.'
```

**Passo 2 — Substituir o bloco atual do Pass 1** (que chama `runClaudePrompt` diretamente e lança exceção se schema inválido) pelo bloco híbrido abaixo:

```typescript
// Pass 1: identify pessoa_principal (no perfil context yet)
const promptPass1 = buildIngestionPrompt({
  teamRegistry,
  perfilMdRaw: null,
  artifactContent: text,
  today,
  managerName,
})

let resultPass1: import('./ClaudeRunner').ClaudeRunnerResult

if (hybridActive) {
  resultPass1 = await runOpenRouterPrompt(
    settings.openRouterApiKey!,
    openRouterModel,
    promptPass1,
    60_000,
    PASS1_SYSTEM_PROMPT,
  )
  if (resultPass1.success && resultPass1.data) {
    const schemaCheck = validateIngestionResult(resultPass1.data)
    if (!schemaCheck.valid) {
      const details = [
        ...schemaCheck.missingFields.map((f) => `campo ausente: ${f}`),
        ...schemaCheck.typeErrors,
      ].join('; ')
      console.warn(`[IngestionPipeline] OpenRouter Pass 1 schema inválido, fallback para Claude CLI: ${details}`)
      resultPass1 = await runClaudePrompt(settings.claudeBinPath, promptPass1, 90_000)
    }
  } else {
    console.warn(`[IngestionPipeline] OpenRouter Pass 1 falhou, fallback para Claude CLI: ${resultPass1.error}`)
    resultPass1 = await runClaudePrompt(settings.claudeBinPath, promptPass1, 90_000)
  }
} else {
  resultPass1 = await runClaudePrompt(settings.claudeBinPath, promptPass1, 90_000)
}

if (!resultPass1.success || !resultPass1.data) {
  throw new Error(resultPass1.error || 'Claude não retornou dados válidos')
}

const validation1 = validateIngestionResult(resultPass1.data)
if (!validation1.valid) {
  const details = [...validation1.missingFields.map((f) => `campo ausente: ${f}`), ...validation1.typeErrors].join('; ')
  throw new Error(`Schema inválido na saída do Claude (pass 1): ${details}`)
}
```

**Importante:** A validação final de `validation1` permanece após o bloco híbrido — ela protege o caso em que o Claude CLI também retornou schema inválido (fallback falhou). Esse é o mesmo comportamento atual: `throw` em schema inválido do Claude CLI.

**Imports:** Verificar que `runOpenRouterPrompt` já está importado no topo do arquivo. Se não estiver, adicionar ao import existente de `ClaudeRunner`:
```typescript
import { runClaudePrompt, runOpenRouterPrompt } from './ClaudeRunner'
```
  </action>

  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - IngestionPipeline.ts contém `if (hybridActive)` dentro de `processItem` no bloco do Pass 1
    - IngestionPipeline.ts contém `runOpenRouterPrompt` sendo chamado com 5 argumentos (incluindo `PASS1_SYSTEM_PROMPT`) no bloco híbrido do Pass 1
    - IngestionPipeline.ts contém `console.warn` com texto `"OpenRouter Pass 1 falhou, fallback para Claude CLI"` para o caso de erro HTTP/network
    - IngestionPipeline.ts contém `console.warn` com texto `"OpenRouter Pass 1 schema inválido, fallback para Claude CLI"` para o caso de schema inválido
    - IngestionPipeline.ts contém `PASS1_SYSTEM_PROMPT` com valor `'You must respond with valid JSON only. Do not include markdown code blocks, explanations, or any text outside the JSON object.'`
    - `npx tsc --noEmit` sai com código 0 (sem erros de tipo)
    - O bloco do Pass 2 (`runClaudePrompt(settings.claudeBinPath, promptPass2, 180_000)`) permanece inalterado
  </acceptance_criteria>

  <done>Pass 1 usa OpenRouter com fallback automático quando hybridActive=true; comportamento inalterado quando hybridActive=false</done>
</task>

</tasks>

<verification>
Verificação end-to-end após ambas as tasks:

1. TypeScript compila sem erros: `npx tsc --noEmit` retorna código 0
2. Padrão de roteamento idêntico ao Pass Cerimônia: buscar `hybridActive` em IngestionPipeline.ts — deve aparecer em dois locais (Pass Cerimônia + Pass 1)
3. `runOpenRouterPrompt` em ClaudeRunner.ts aceita 5 parâmetros; callers existentes com 4 parâmetros continuam válidos (TypeScript confirma via `--noEmit`)
4. Pass 2 não foi tocado — `runClaudePrompt(settings.claudeBinPath, promptPass2, 180_000)` deve existir exatamente como antes
</verification>

<success_criteria>
- Com `useHybridModel=true` e `openRouterApiKey` preenchida: Pass 1 chama OpenRouter com system prompt de JSON; fallback automático para Claude CLI em qualquer falha (HTTP, network, schema)
- Com `useHybridModel=false` ou sem `openRouterApiKey`: Pass 1 chama Claude CLI diretamente — zero mudança de comportamento
- TypeScript strict: `npx tsc --noEmit` passa sem erros
- Pass 2 e Pass Cerimônia permanecem intocados
- Nenhuma mudança de UI, schema ou IPC — purely backend, conforme escopo da fase
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.4-openrouter-estagio-2-pass-1-modelo-leve/999.4-01-SUMMARY.md` com:
- Decisões implementadas (D-01 a D-05)
- Arquivos modificados com diff resumido
- Comportamento observável antes/depois
- Qualquer desvio do plano com justificativa
</output>
