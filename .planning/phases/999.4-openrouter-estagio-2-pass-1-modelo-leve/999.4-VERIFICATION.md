---
phase: 999.4-openrouter-estagio-2-pass-1-modelo-leve
verified: 2026-03-27T15:00:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 999.4: Pass 1 com Rota Híbrida OpenRouter — Verification Report

**Phase Goal:** Migrar o Pass 1 (identificação de pessoa_principal e metadados básicos) para OpenRouter quando o modelo híbrido estiver ativo. Padrão já estabelecido no Pass Cerimônia (roteamento condicional + fallback para Claude CLI).
**Verified:** 2026-03-27T15:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Com useHybridModel=true e openRouterApiKey preenchida, Pass 1 chama OpenRouter em vez de Claude CLI | VERIFIED | IngestionPipeline.ts L961-968: `if (hybridActive) { resultPass1 = await runOpenRouterPrompt(...)` |
| 2 | Se OpenRouter retornar erro HTTP ou network error, Pass 1 executa fallback automático para Claude CLI com log de warning | VERIFIED | IngestionPipeline.ts L979-981: `console.warn('[IngestionPipeline] OpenRouter Pass 1 falhou, fallback para Claude CLI: ...')` + `runClaudePrompt(...)` |
| 3 | Se OpenRouter retornar JSON com schema inválido (validateIngestionResult falhar), Pass 1 executa fallback para Claude CLI | VERIFIED | IngestionPipeline.ts L970-978: schemaCheck via `validateIngestionResult`, warn + fallback when `!schemaCheck.valid` |
| 4 | Com useHybridModel=false ou sem openRouterApiKey, Pass 1 continua usando Claude CLI sem nenhuma mudança de comportamento | VERIFIED | IngestionPipeline.ts L983-984: `else { resultPass1 = await runClaudePrompt(settings.claudeBinPath, promptPass1, 90_000) }` |
| 5 | O payload OpenRouter para Pass 1 inclui campo system com instrução de output JSON puro | VERIFIED | IngestionPipeline.ts L948: `PASS1_SYSTEM_PROMPT = 'You must respond with valid JSON only...'`; ClaudeRunner.ts L151-153: spread `systemPrompt` into messages array |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/ingestion/ClaudeRunner.ts` | runOpenRouterPrompt com parâmetro opcional systemPrompt; exports runOpenRouterPrompt, runClaudePrompt, ClaudeRunnerResult | VERIFIED | L126-132: signature `runOpenRouterPrompt(apiKey, model, prompt, timeoutMs, systemPrompt?)` — 5th parameter optional. Exports confirmed at L3, L18, L126. |
| `src/main/ingestion/IngestionPipeline.ts` | Rota híbrida no Pass 1 dentro de processItem com `if (hybridActive)` | VERIFIED | L946-985: full hybrid block with PASS1_SYSTEM_PROMPT, dual fallback paths, `if (hybridActive)` at L961. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| IngestionPipeline.ts (processItem) | ClaudeRunner.ts (runOpenRouterPrompt) | chamada condicional com hybridActive | WIRED | L962: `runOpenRouterPrompt(settings.openRouterApiKey!, openRouterModel, promptPass1, 60_000, PASS1_SYSTEM_PROMPT)` — 5 arguments confirmed |
| IngestionPipeline.ts (processItem) | SchemaValidator.ts (validateIngestionResult) | validação pós-OpenRouter que aciona fallback | WIRED | L970: `validateIngestionResult(resultPass1.data)` inside hybridActive block; pattern `schemaCheck.valid` at L971 |

---

### Data-Flow Trace (Level 4)

Pass 1 is a backend routing/orchestration block, not a UI component rendering dynamic data. Level 4 data-flow trace is not applicable — there is no state variable flowing to a renderer. The pipeline returns a structured result (`IngestionAIResult`) consumed downstream by the persisted profile; the routing logic itself is what was verified in Levels 1-3.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compila sem erros | `npx tsc --noEmit` | EXIT_CODE=0, no output | PASS |
| hybridActive aparece em Pass 1 (L946) e Pass Cerimônia (L291, L405) | `grep -n "hybridActive = " IngestionPipeline.ts` | 3 declaration sites confirmed | PASS |
| runOpenRouterPrompt chamado com 5 argumentos no bloco Pass 1 | `grep -n "runOpenRouterPrompt" IngestionPipeline.ts` | L962-968: 5 args including PASS1_SYSTEM_PROMPT | PASS |
| Pass 2 permanece intocado (180s timeout, Claude CLI direto) | `grep -n "promptPass2" IngestionPipeline.ts` | L1014: `runClaudePrompt(settings.claudeBinPath, promptPass2, 180_000)` unchanged | PASS |
| Commits documentados existem no histórico git | `git show --stat 02987cd 71b6f6a` | Both commits found, correct files | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PERF-01 | 999.4-01-PLAN.md | Gestor ativa modelo híbrido via Settings e observa redução de latência de ingestão. Critérios: (1) campos SettingsView persistidos; (2) Pass Cerimônia chama OpenRouter com híbrido ativo; (3) fallback automático para Claude CLI com log de warning. | PARTIALLY SATISFIED (extended) | PERF-01 was marked Complete in Phase 999.3 (covering criteria 1-3 for Pass Cerimônia). Phase 999.4 extends the same requirement by migrating Pass 1 — the same toggle, same fallback pattern. The requirement text maps to Pass Cerimônia; Phase 999.4 goes beyond the literal criteria to cover Pass 1 as well. No new requirement ID was defined. |

**Orphaned requirements:** None. REQUIREMENTS.md maps PERF-01 to Phase 999.3 (Complete). Phase 999.4 is listed in ROADMAP.md under `Requirements: PERF-01` as an extension of PERF-01 scope. The plan's claim is consistent: same requirement, extended scope.

**Note on requirement ID reuse:** PERF-01 is formally marked Complete (Phase 999.3) in REQUIREMENTS.md traceability table. Phase 999.4 extends Pass 1 routing under the same ID without creating a new requirement entry. This is an acceptable practice for incremental pipeline migration within the same performance goal — no blocking issue.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TODO/FIXME/placeholder/empty return found | — | — |

No anti-patterns detected in `ClaudeRunner.ts` or `IngestionPipeline.ts`. The `console.warn` calls in the hybrid block are intentional warning logs (documented in the plan as observable evidence of fallback), not debug stubs.

---

### Human Verification Required

#### 1. End-to-End Hybrid Ingest with Real OpenRouter Key

**Test:** Configure `useHybridModel=true`, set a valid `openRouterApiKey` and `openRouterModel` in Settings. Drop an artifact into the inbox and trigger ingest. Observe main process logs.
**Expected:** Logs show `[OpenRouter] model=google/gemma-3-27b-it ... bytes` and then the artifact is persisted correctly with a valid `pessoa_principal`.
**Why human:** Cannot run Electron app or call external API in programmatic verification.

#### 2. Fallback Trigger via Invalid API Key

**Test:** Configure `useHybridModel=true` with a deliberately invalid `openRouterApiKey`. Trigger ingest.
**Expected:** Logs show `[IngestionPipeline] OpenRouter Pass 1 falhou, fallback para Claude CLI: OpenRouter HTTP 401: ...` and ingest completes normally via Claude CLI fallback.
**Why human:** Requires running the app with intentional bad credentials and observing log output.

#### 3. Schema-Invalidity Fallback (Model Returns Malformed JSON)

**Test:** Cannot trivially force a model to return schema-invalid JSON in integration — requires either mocking or a specific model known to produce partial output.
**Expected:** `[IngestionPipeline] OpenRouter Pass 1 schema inválido, fallback para Claude CLI: ...` appears in logs and ingest still succeeds.
**Why human:** Requires controlled model output manipulation or integration test harness.

---

### Gaps Summary

No gaps. All five must-have truths verified, both artifacts exist with substantive implementation, both key links are wired, TypeScript compiles clean, and documented commits (02987cd, 71b6f6a) are present in git history with correct file changes. The requirement PERF-01 is satisfied (it was already marked Complete in Phase 999.3; Phase 999.4 extends the same toggle/fallback pattern to Pass 1). Phase goal achieved.

---

_Verified: 2026-03-27T15:00:00Z_
_Verifier: Claude (gsd-verifier)_
