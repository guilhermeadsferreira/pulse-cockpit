---
phase: 03-github-metrics-crossanalyzer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/external/CrossAnalyzer.ts
  - src/main/external/ExternalDataPass.ts
autonomous: true
requirements: [MTRC-04, MTRC-05]

must_haves:
  truths:
    - "Insights do CrossAnalyzer incluem campo causa_raiz quando tipo e desalinhamento, sobrecarga ou gap_comunicacao"
    - "CrossAnalyzer verifica contexto do perfil (ferias, licenca, ausencia) antes de flaggar desalinhamento por queda de atividade"
  artifacts:
    - path: "src/main/external/CrossAnalyzer.ts"
      provides: "Campo causa_raiz nos insights e checagem de contexto de ausencia"
      contains: "causa_raiz"
    - path: "src/main/external/ExternalDataPass.ts"
      provides: "Extracao de contexto do perfil.md para alimentar CrossAnalyzer"
      contains: "extractProfileContext"
  key_links:
    - from: "src/main/external/ExternalDataPass.ts"
      to: "src/main/external/CrossAnalyzer.ts"
      via: "profileContext passado para analyze()"
      pattern: "profileContext"
    - from: "src/main/external/CrossAnalyzer.ts"
      to: "CrossInsight"
      via: "campo causa_raiz no tipo"
      pattern: "causa_raiz"
---

<objective>
Enriquecer CrossAnalyzer com campo causa_raiz nos insights (MTRC-04) e checagem de contexto do perfil — ferias, licenca, ausencia — antes de flaggar desalinhamento (MTRC-05).

Purpose: Evitar falsos positivos de desalinhamento quando o liderado esta de ferias ou licenca, e dar ao gestor a causa raiz do problema (awaiting review, changes requested, stale).
Output: CrossInsight com campo causa_raiz e logica de supressao de alertas por contexto de ausencia.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/main/external/CrossAnalyzer.ts
@src/main/external/ExternalDataPass.ts
@src/main/registry/PersonRegistry.ts

<interfaces>
From src/main/external/CrossAnalyzer.ts:
```typescript
export interface CrossInsight {
  tipo: 'sobrecarga' | 'desalinhamento' | 'gap_comunicacao' | 'crescimento' | 'bloqueio' | 'risco_sprint' | 'destaque'
  severidade: 'alta' | 'media' | 'baixa'
  descricao: string
  evidencia: string
  acaoSugerida?: string
  gerarDemanda?: boolean
}

export interface CrossAnalyzerInput {
  jira: JiraPersonMetrics | null
  github: GitHubPersonMetrics | null
  previousJira: Partial<JiraPersonMetrics> | null
  previousGithub: Partial<GitHubPersonMetrics> | null
}

export function analyze(input: CrossAnalyzerInput, thresholds?, nivel?): CrossInsight[]
```

From src/main/external/ExternalDataPass.ts:
```typescript
export interface ExternalDataSnapshot {
  jira: JiraPersonMetrics | null
  github: GitHubPersonMetrics | null
  insights: CrossInsight[]
  atualizadoEm: string
}
// analyze() called at line 183 with (analysisInput, undefined, person.nivel)
```

From src/main/registry/PersonRegistry.ts:
```typescript
export interface PersonConfig {
  // ... (no vacation/leave fields currently)
  slug: string; nome: string; cargo: string; nivel: string;
  notas_manuais?: string; alerta_ativo: boolean; motivo_alerta?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar causa_raiz ao CrossInsight e logica de contexto de ausencia</name>
  <files>src/main/external/CrossAnalyzer.ts, src/main/external/ExternalDataPass.ts</files>
  <read_first>src/main/external/CrossAnalyzer.ts, src/main/external/ExternalDataPass.ts, src/main/registry/PersonRegistry.ts</read_first>
  <action>
**Parte A — CrossAnalyzer.ts:**

1. Adicionar campo `causa_raiz` ao `CrossInsight`:
```typescript
export interface CrossInsight {
  tipo: 'sobrecarga' | 'desalinhamento' | 'gap_comunicacao' | 'crescimento' | 'bloqueio' | 'risco_sprint' | 'destaque'
  severidade: 'alta' | 'media' | 'baixa'
  descricao: string
  evidencia: string
  acaoSugerida?: string
  gerarDemanda?: boolean
  causa_raiz?: 'awaiting_review' | 'changes_requested' | 'stale' | 'blocked' | 'overloaded' | 'vacation' | 'leave' | null
}
```

2. Adicionar interface `ProfileContext` para receber contexto do perfil:
```typescript
export interface ProfileContext {
  emFerias: boolean
  emLicenca: boolean
  ausenciaDescricao?: string
}
```

3. Alterar assinatura de `analyze` para receber `profileContext?: ProfileContext` como 4o parametro:
```typescript
export function analyze(
  input: CrossAnalyzerInput,
  thresholds?: Partial<CrossAnalyzerThresholds>,
  nivel?: string,
  profileContext?: ProfileContext,
): CrossInsight[]
```

4. Dentro de `analyze`, ANTES de rodar as analises:
   - Se `profileContext?.emFerias || profileContext?.emLicenca`, suprimir insights de tipo 'desalinhamento' e 'gap_comunicacao':
     ```typescript
     if (profileContext?.emFerias || profileContext?.emLicenca) {
       // Skip activity drop and communication gap analysis
       // Only run overload/blocker/sprint risk (still relevant for return planning)
     }
     ```
   - Implementar via flag `skipActivityAnalysis` que desabilita chamadas a `analyzeActivityDrop`, `analyzeCommunicationGap` e `analyzeGrowth` quando pessoa esta ausente

5. Adicionar `causa_raiz` nos insights existentes:
   - `analyzeOverload`: causa_raiz = 'overloaded'
   - `analyzeBlockers`: causa_raiz = 'blocked'
   - `analyzePRAccumulation`: causa_raiz = 'awaiting_review' (se PRs abertos estao aguardando review — usar `github.prsAbertos > 0`) ou 'stale' (se `tempoMedioAbertoDias > 7`)
   - `analyzeCommunicationGap`: causa_raiz = 'stale' (issues sem codigo) ou causa_raiz = null (PRs sem issues)
   - `analyzeActivityDrop`: causa_raiz = (profileContext?.emFerias ? 'vacation' : profileContext?.emLicenca ? 'leave' : 'stale')
   - `analyzeGrowth` e `analyzeHighlights`: causa_raiz = null (nao aplicavel)

**Parte B — ExternalDataPass.ts:**

1. Adicionar metodo `extractProfileContext(slug: string): ProfileContext`:
   - Le `perfil.md` do slug
   - Procura no conteudo por patterns indicando ausencia:
     - `ferias` ou `férias` (case insensitive) nas ultimas 5 linhas de `## Notas` ou `notas_manuais`
     - `licenca` ou `licença` (case insensitive) nas ultimas 5 linhas de `## Notas` ou `notas_manuais`
     - `ausente` ou `afastado` (case insensitive)
   - Tambem checar campo `notas_manuais` do PersonConfig (via registry.get(slug))
   - Retornar `{ emFerias: boolean, emLicenca: boolean, ausenciaDescricao?: string }`
   - Se nenhum pattern encontrado, retornar `{ emFerias: false, emLicenca: false }`

2. Na funcao `fetchAndAnalyze`, antes de chamar `analyze()`:
   - Chamar `this.extractProfileContext(person.slug)` — importar ProfileContext de CrossAnalyzer
   - Passar como 4o argumento: `analyze(analysisInput, undefined, person.nivel, profileContext)`
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CrossInsight interface contains `causa_raiz?:` field
    - CrossAnalyzer.ts contains `export interface ProfileContext`
    - analyze function signature has 4th parameter `profileContext?: ProfileContext`
    - analyzePRAccumulation sets causa_raiz to 'awaiting_review' or 'stale'
    - analyzeBlockers sets causa_raiz to 'blocked'
    - analyzeOverload sets causa_raiz to 'overloaded'
    - ExternalDataPass.ts contains `extractProfileContext(`
    - ExternalDataPass fetchAndAnalyze calls `this.extractProfileContext(`
    - analyze call in fetchAndAnalyze passes profileContext as 4th arg
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>CrossInsight tem campo causa_raiz preenchido em todos os insights relevantes, e desalinhamento/gap_comunicacao sao suprimidos quando pessoa esta de ferias ou licenca</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- CrossInsight.causa_raiz esta presente na interface
- ExternalDataPass.extractProfileContext retorna ProfileContext
- Callers existentes de analyze() continuam funcionando (parametro opcional)
</verification>

<success_criteria>
- MTRC-04: Insights incluem causa_raiz (awaiting_review, changes_requested, stale, blocked, overloaded, vacation, leave)
- MTRC-05: Pessoa em ferias/licenca nao recebe insights de desalinhamento ou gap_comunicacao
- Compilacao TypeScript passa sem erros
</success_criteria>

<output>
After completion, create `.planning/phases/03-github-metrics-crossanalyzer/03-02-SUMMARY.md`
</output>
