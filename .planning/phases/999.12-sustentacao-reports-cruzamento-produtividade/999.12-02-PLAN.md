---
phase: 999.12-sustentacao-reports-cruzamento-produtividade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/external/WeeklyReportGenerator.ts
  - src/main/external/MetricsWriter.ts
autonomous: true
requirements:
  - SRCP-04
  - SRCP-05

must_haves:
  truths:
    - "O Weekly Report contém uma seção '## Sustentação da Semana' com carga total, breaches e SLA compliance"
    - "A seção Sustentação do Weekly cruza porAssignee com os liderados — mostrando quem absorveu carga de suporte na semana"
    - "MetricsWriter tem método writeSustentacaoWeekly() que persiste carga de suporte por pessoa no metricas.md"
    - "WeeklyReportGenerator chama writeSustentacaoWeekly() para cada pessoa com tickets no porAssignee"
    - "Se sustentação não está configurada, seção é omitida silenciosamente"
  artifacts:
    - path: "src/main/external/MetricsWriter.ts"
      provides: "SustentacaoWeeklyEntry interface e writeSustentacaoWeekly() method"
      exports: ["SustentacaoWeeklyEntry", "writeSustentacaoWeekly"]
      contains: "SUSTENTACAO_SEMANAL"
    - path: "src/main/external/WeeklyReportGenerator.ts"
      provides: "integração com SupportBoardSnapshot na geração do weekly"
      contains: "Sustentação da Semana"
  key_links:
    - from: "src/main/external/WeeklyReportGenerator.ts"
      to: "src/main/external/SupportBoardClient.ts"
      via: "fetchSustentacaoForReport() chamada no generate()"
      pattern: "fetchSustentacaoForReport"
    - from: "src/main/external/WeeklyReportGenerator.ts"
      to: "src/main/external/MetricsWriter.ts"
      via: "metricsWriter.writeSustentacaoWeekly() para cada pessoa com tickets"
      pattern: "writeSustentacaoWeekly"
---

<objective>
Integrar dados de sustentação no Weekly Report e persistir carga de suporte por pessoa no metricas.md via novo método MetricsWriter.writeSustentacaoWeekly().

Purpose: Assim como o Daily captura snapshot diário de suporte, o Weekly consolida carga de sustentação na semana, criando visibilidade de quem absorveu suporte em paralelo ao trabalho de sprint. Persistir no metricas.md permite análise retroativa de tendência.

Output: WeeklyReportGenerator com seção de Sustentação; MetricsWriter com nova seção SUSTENTACAO_SEMANAL por pessoa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/main/external/WeeklyReportGenerator.ts
@src/main/external/MetricsWriter.ts
@src/main/external/SupportBoardClient.ts
@src/renderer/src/types/ipc.ts

<interfaces>
<!-- De src/main/external/MetricsWriter.ts — padrão existente de seções gerenciadas -->
```typescript
// Padrão de seção gerenciada — SEGUIR EXATAMENTE
const SECTIONS = {
  MOMENTO_ATUAL:      { open: '<!-- METRICAS:MOMENTO_ATUAL -->',      close: '<!-- FIM:MOMENTO_ATUAL -->' },
  ALERTAS:            { open: '<!-- METRICAS:ALERTAS -->',            close: '<!-- FIM:ALERTAS -->' },
  SEMANAS:            { open: '<!-- METRICAS:SEMANAS -->',            close: '<!-- FIM:SEMANAS -->' },
  SPRINTS:            { open: '<!-- METRICAS:SPRINTS -->',            close: '<!-- FIM:SPRINTS -->' },
  MESES:              { open: '<!-- METRICAS:MESES -->',              close: '<!-- FIM:MESES -->' },
  SUSTENTACAO_ANALISE: { open: '<!-- METRICAS:SUSTENTACAO_ANALISE -->', close: '<!-- FIM:SUSTENTACAO_ANALISE -->' },
} as const

// Padrão de upsertSection — método privado existente
private upsertSection(content: string, key: keyof typeof SECTIONS, newBlock: string): string
// Retorna content com a seção substituída ou appended

// Tipos existentes como referência de padrão
export interface WeeklyEntry {
  semana: string
  velocity: number
  deltaSP: number | null
  prsMerged: number
  reviews: number
  collaborationScore: number
  cycleTimeMedio: number
}
```

<!-- De src/main/external/SupportBoardClient.ts (criada no Plan 01) -->
```typescript
export async function fetchSustentacaoForReport(settings: AppSettings): Promise<SupportBoardSnapshot | null>
```

<!-- De src/main/external/WeeklyReportGenerator.ts — estrutura PersonWeeklyData e buildReport -->
```typescript
interface PersonWeeklyData {
  nome: string
  slug: string
  snapshot: ExternalDataSnapshot | null
  weeklyGithub: WeeklyGitHubData
  previous: WeeklyPreviousData
  narrativeContext: string
  baseline: { avgCommits: number; avgPRsMerged: number; avgReviews: number } | null
}

// buildReport recebe personReports e constrói string final
private buildReport(personReports: PersonWeeklyData[], start: string, end: string): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar SustentacaoWeeklyEntry e writeSustentacaoWeekly() ao MetricsWriter</name>
  <files>src/main/external/MetricsWriter.ts</files>
  <action>
1. Adicionar nova interface exportada `SustentacaoWeeklyEntry` após `MomentoAtualEntry`:

```typescript
export interface SustentacaoWeeklyEntry {
  semana: string        // formato "YYYY-MM-DD a YYYY-MM-DD"
  ticketsAbertos: number
  breachCount: number
  complianceRate7d: number | null
}
```

2. Adicionar chave `SUSTENTACAO_SEMANAL` no objeto `SECTIONS`:
```typescript
SUSTENTACAO_SEMANAL: { open: '<!-- METRICAS:SUSTENTACAO_SEMANAL -->', close: '<!-- FIM:SUSTENTACAO_SEMANAL -->' },
```

3. Adicionar método público `writeSustentacaoWeekly(slug: string, entry: SustentacaoWeeklyEntry): void` seguindo o padrão exato de `writeWeekly()`:
   - Lê o arquivo do slug com `this.ensureFile(slug)`
   - Lê conteúdo com `readFileSync`
   - Chama `this.upsertSection(content, 'SUSTENTACAO_SEMANAL', newBlock)` onde `newBlock` é uma linha formatada
   - Grava com `writeFileSync`
   - Trata erro com `log.warn` e não lança exceção

Formato da linha a ser prependada na seção:
```
| {entry.semana} | {entry.ticketsAbertos} | {entry.breachCount} | {complianceStr} |
```
Onde `complianceStr` é `${entry.complianceRate7d}%` ou `—` quando null.

O bloco gerado deve seguir o mesmo padrão tabular de `writeWeekly()`:
```markdown
<!-- METRICAS:SUSTENTACAO_SEMANAL -->
### Sustentação Semanal

| Semana | Abertos | Breach | SLA 7d |
|--------|---------|--------|--------|
| {semana} | {ticketsAbertos} | {breachCount} | {complianceRate7d}% |
{entradas anteriores}
<!-- FIM:SUSTENTACAO_SEMANAL -->
```

Reter apenas as últimas 12 entradas (3 meses de semanas) — mesma lógica de retenção de `writeWeekly()`.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - SustentacaoWeeklyEntry exportada
    - SUSTENTACAO_SEMANAL no SECTIONS
    - writeSustentacaoWeekly() implementada, compila sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrar sustentação no WeeklyReportGenerator</name>
  <files>src/main/external/WeeklyReportGenerator.ts</files>
  <action>
1. Adicionar imports no topo do arquivo:
```typescript
import { fetchSustentacaoForReport } from './SupportBoardClient'
import type { SupportBoardSnapshot } from '../../renderer/src/types/ipc'
import type { SustentacaoWeeklyEntry } from './MetricsWriter'
```

2. No método `generate()`, após o loop de coleta de dados de pessoas (antes de `progress('build', ...)`), adicionar fetch de sustentação:
```typescript
// Fetch sustentação (graceful — null se não configurado)
progress('sustentacao', 'Buscando dados de sustentação…', 72)
let sustentacaoSnapshot: SupportBoardSnapshot | null = null
try {
  sustentacaoSnapshot = await fetchSustentacaoForReport(settings)
} catch (err) {
  log.warn('sustentação: falhou (graceful)', { error: err instanceof Error ? err.message : String(err) })
}
```

3. Passar `sustentacaoSnapshot` para `buildReport()` — estender a assinatura:
```typescript
private buildReport(
  personReports: PersonWeeklyData[],
  start: string,
  end: string,
  sustentacao?: SupportBoardSnapshot | null,
): string
```

4. Atualizar a chamada: `this.buildReport(personReports, start, end, sustentacaoSnapshot)`

5. Dentro de `buildReport()`, após a seção `## Resumo do Time`, adicionar seção de Sustentação quando snapshot não-null:

```typescript
if (sustentacao) {
  lines.push('## Sustentação da Semana', '')
  lines.push(`> Abertos: **${sustentacao.ticketsAbertos}** | Breach: **${sustentacao.ticketsEmBreach.length}**${sustentacao.complianceRate7d !== null ? ` | SLA 7d: **${sustentacao.complianceRate7d}%**` : ''}`, '')

  const assigneeEntries = Object.entries(sustentacao.porAssignee)
    .filter(([, count]) => count > 0)
    .sort((a, b) => b[1] - a[1])

  if (assigneeEntries.length > 0) {
    lines.push('### Carga por pessoa', '')
    for (const [slug, count] of assigneeEntries) {
      const person = personReports.find(p => p.slug === slug)
      const nome = person?.nome ?? slug
      const alert = count >= 5 ? ' ⚠️ alta carga' : count >= 3 ? ' 🔵 carga moderada' : ''
      lines.push(`- **${nome}**: ${count} ticket(s)${alert}`)

      // Cruzamento com produtividade: se a pessoa tem velocity baixa E alta carga
      const snap = person?.snapshot?.jira
      if (snap && count >= 3) {
        const spDone = snap.storyPointsSprint ?? 0
        if (spDone === 0) {
          lines.push(`  > _Zero SP entregues na semana — possível impacto de ${count} tickets de suporte_`)
        }
      }
    }
    lines.push('')
  }

  if (sustentacao.topTipos.length > 0) {
    const tiposStr = sustentacao.topTipos.slice(0, 3).map(t => `${t.tipo} (${t.count})`).join(', ')
    lines.push(`> Top tipos: ${tiposStr}`, '')
  }
}
```

6. Após o loop de persistência de metricas semanais (MetricsWriter), adicionar persistência de sustentação por pessoa:
```typescript
// Persistir carga de sustentação por pessoa no metricas.md
if (sustentacaoSnapshot) {
  for (const [slug, count] of Object.entries(sustentacaoSnapshot.porAssignee)) {
    if (count === 0) continue
    try {
      const entry: SustentacaoWeeklyEntry = {
        semana: `${start} a ${end}`,
        ticketsAbertos: count,
        breachCount: sustentacaoSnapshot.ticketsEmBreach.filter(t => t.assignee === slug).length,
        complianceRate7d: sustentacaoSnapshot.complianceRate7d,
      }
      metricsWriter.writeSustentacaoWeekly(slug, entry)
    } catch (err) {
      log.warn('falha ao persistir sustentacao semanal', { slug, error: err instanceof Error ? err.message : String(err) })
    }
  }
}
```

ATENÇÃO: o `porAssignee` usa slug como chave (definido em SUST-01). Confirmar que o loop usa `slug` diretamente para `metricsWriter.writeSustentacaoWeekly(slug, ...)` — sem transformação.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - WeeklyReportGenerator.ts compila sem erros
    - buildReport aceita parâmetro sustentacao opcional
    - Seção "Sustentação da Semana" adicionada ao report com cruzamento produtividade
    - writeSustentacaoWeekly() chamado para cada pessoa com tickets
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passa sem erros
2. `grep -n "SUSTENTACAO_SEMANAL" src/main/external/MetricsWriter.ts` — encontra a chave na constante SECTIONS
3. `grep -n "writeSustentacaoWeekly" src/main/external/MetricsWriter.ts` — encontra o método
4. `grep -n "Sustentação da Semana" src/main/external/WeeklyReportGenerator.ts` — encontra a seção
5. `grep -n "writeSustentacaoWeekly" src/main/external/WeeklyReportGenerator.ts` — encontra a chamada
</verification>

<success_criteria>
- SustentacaoWeeklyEntry exportada do MetricsWriter
- writeSustentacaoWeekly() persiste dados tabulares no metricas.md com retenção de 12 entradas
- Weekly Report inclui seção de Sustentação quando board configurado
- Cruzamento inline quando pessoa tem >= 3 tickets E zero SP na semana
- Compila sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.12-sustentacao-reports-cruzamento-produtividade/999.12-02-SUMMARY.md`
</output>
