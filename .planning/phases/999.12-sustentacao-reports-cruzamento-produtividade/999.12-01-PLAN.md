---
phase: 999.12-sustentacao-reports-cruzamento-produtividade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/external/SupportBoardClient.ts
  - src/main/external/DailyReportGenerator.ts
  - src/main/prompts/daily-analysis.prompt.ts
autonomous: true
requirements:
  - SRCP-01
  - SRCP-02
  - SRCP-03

must_haves:
  truths:
    - "O Daily Report contém uma seção '## Sustentação' com carga total, breach count e SLA compliance rate"
    - "A seção Sustentação cruza porAssignee com os nomes das pessoas do time — mostrando quem está sobrecarregado"
    - "A análise de IA (Haiku) recebe dados de sustentação como input adicional e gera observações de correlação produtividade×suporte"
    - "Se sustentação não está configurada ou fetchAndCacheSustentacao retorna null, a seção é omitida silenciosamente"
  artifacts:
    - path: "src/main/external/SupportBoardClient.ts"
      provides: "função exportada fetchSustentacaoForReport() que busca snapshot sem depender de IPC"
      exports: ["fetchSustentacaoForReport"]
    - path: "src/main/external/DailyReportGenerator.ts"
      provides: "integração com SupportBoardSnapshot na geração do daily"
      contains: "Sustentação"
    - path: "src/main/prompts/daily-analysis.prompt.ts"
      provides: "campo sustentacao? no DailyAnalysisInput e instrução de cruzamento"
      contains: "sustentacao"
  key_links:
    - from: "src/main/external/DailyReportGenerator.ts"
      to: "src/main/external/SupportBoardClient.ts"
      via: "fetchSustentacaoForReport() chamada no generate()"
      pattern: "fetchSustentacaoForReport"
    - from: "src/main/external/DailyReportGenerator.ts"
      to: "src/main/prompts/daily-analysis.prompt.ts"
      via: "analysisInput.sustentacao passado para buildDailyAnalysisPrompt"
      pattern: "sustentacao"
---

<objective>
Integrar dados de sustentação no Daily Report: seção dedicada com carga total, breaches e SLA compliance, cruzamento de porAssignee com o time, e dados de sustentação injetados no prompt da análise de IA.

Purpose: O gestor verá no daily não apenas produtividade de sprint, mas também carga de suporte de cada pessoa — "Fulano teve velocity baixa E estava carregando 8 tickets de suporte" torna-se visível.

Output: DailyReportGenerator gera seção Sustentação; análise de IA recebe contexto de suporte para correlacionar com dados de produtividade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/external/SupportBoardClient.ts
@src/main/external/DailyReportGenerator.ts
@src/main/prompts/daily-analysis.prompt.ts
@src/renderer/src/types/ipc.ts

<interfaces>
<!-- Tipos existentes relevantes — extraídos de src/renderer/src/types/ipc.ts -->

```typescript
export interface SupportBoardSnapshot {
  atualizadoEm: string
  ticketsAbertos: number
  ticketsFechadosUltimos30d: number
  topTipos: Array<{ tipo: string; count: number }>
  topLabels: Array<{ label: string; count: number }>
  ticketsEmBreach: SupportTicket[]
  /** slug → contagem de tickets abertos */
  porAssignee: Record<string, number>
  complianceRate7d: number | null
  complianceRate30d: number | null
  history: SustentacaoHistoryEntry[]
}

export interface SupportTicket {
  key: string
  summary: string
  type: string
  assignee: string | null
  ageDias: number
  status: string
  slaBreached: boolean
  recentComments: Array<{ author: string; body: string; created: string }>
}
```

<!-- De src/main/external/SupportBoardClient.ts -->
```typescript
// Função existente — busca e calcula snapshot completo
export async function fetchSupportBoardMetrics(input: SupportBoardInput): Promise<SupportBoardSnapshot>

// SupportBoardInput:
interface SupportBoardInput {
  config: JiraConfig
  projectKey: string
  slaThresholds?: Record<string, number>
}
```

<!-- De src/main/external/DailyReportGenerator.ts — assinatura buildReport e runHaikuAnalysis -->
```typescript
private buildReport(
  personReports: PersonDailyData[],
  sprintOverview: SprintOverview | null,
  today: string,
  allActiveTasks?: TaskCycleInfo[],
  doneTasksPhaseTime?: Map<string, number[]>,
): { content: string; analysisInput: { sprintOverview: string; perPersonSummary: string; alerts: string; pipelineHealth?: string } }

private async runHaikuAnalysis(
  settings: AppSettings,
  analysisInput: { sprintOverview: string; perPersonSummary: string; alerts: string; pipelineHealth?: string },
): Promise<string | null>
```

<!-- De src/main/registry/SettingsManager.ts — campos relevantes de AppSettings -->
```typescript
interface AppSettings {
  jiraEnabled?: boolean
  jiraBaseUrl?: string
  jiraEmail?: string
  jiraApiToken?: string
  jiraBoardId?: number
  jiraSupportBoardId?: number
  jiraSupportProjectKey?: string
  jiraSlaThresholds?: Record<string, number>
  claudeBinPath?: string
  claudeDefaultModel?: string
}
```

<!-- De src/main/registry/PersonRegistry.ts -->
```typescript
interface PersonConfig {
  slug: string
  nome: string
  jiraEmail?: string
  githubUsername?: string
  relacao: 'liderado' | 'par' | 'gestor'
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exportar fetchSustentacaoForReport() no SupportBoardClient</name>
  <files>src/main/external/SupportBoardClient.ts</files>
  <action>
Adicionar função exportada `fetchSustentacaoForReport` que pode ser chamada diretamente pelos generators sem passar pelo IPC handler. A função deve:

1. Receber `settings: AppSettings` como parâmetro (importar `AppSettings` de `'../registry/SettingsManager'` — já importado indiretamente; usar `import type { AppSettings } from '../registry/SettingsManager'`)
2. Checar se sustentação está configurada: `settings.jiraEnabled && settings.jiraBaseUrl && settings.jiraApiToken && settings.jiraSupportProjectKey`
3. Se não configurada, retornar `null`
4. Construir `JiraConfig` e chamar `fetchSupportBoardMetrics()` com `projectKey = settings.jiraSupportProjectKey` e `slaThresholds = settings.jiraSlaThresholds ?? {}`
5. Em caso de erro, logar com `log.warn` e retornar `null` (graceful degradation — nunca lançar exceção)
6. NÃO ler nem gravar history.json (isso é responsabilidade do IPC handler em index.ts). Retornar snapshot com `history: []`

```typescript
export async function fetchSustentacaoForReport(settings: AppSettings): Promise<SupportBoardSnapshot | null> {
  if (!settings.jiraEnabled || !settings.jiraBaseUrl || !settings.jiraApiToken || !settings.jiraSupportProjectKey) {
    return null
  }
  try {
    const config: JiraConfig = {
      baseUrl: settings.jiraBaseUrl,
      email: settings.jiraEmail ?? '',
      apiToken: settings.jiraApiToken,
    }
    return await fetchSupportBoardMetrics({
      config,
      projectKey: settings.jiraSupportProjectKey,
      slaThresholds: settings.jiraSlaThresholds ?? {},
    })
  } catch (err) {
    log.warn('fetchSustentacaoForReport: falhou (graceful)', { error: err instanceof Error ? err.message : String(err) })
    return null
  }
}
```

Adicionar `import type { AppSettings } from '../registry/SettingsManager'` no topo do arquivo (após os imports existentes).
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>fetchSustentacaoForReport exportada, compila sem erros TypeScript, retorna null quando settings incompletos</done>
</task>

<task type="auto">
  <name>Task 2: Integrar sustentação no DailyReportGenerator e no prompt de IA</name>
  <files>src/main/external/DailyReportGenerator.ts, src/main/prompts/daily-analysis.prompt.ts</files>
  <action>
**Parte A — daily-analysis.prompt.ts:**

Estender a interface `DailyAnalysisInput` com campo opcional `sustentacao?: string` e adicionar a seção no prompt:

```typescript
interface DailyAnalysisInput {
  sprintOverview: string
  perPersonSummary: string
  alerts: string
  pipelineHealth?: string
  sustentacao?: string   // NOVO
}
```

No corpo do prompt, após `### Pipeline Health`, adicionar:
```
### Sustentação (board de suporte)
${input.sustentacao || 'Sem dados de sustentação.'}
```

Na lista de instruções de análise, adicionar item sobre cruzamento:
```
6. **Cruzamento sustentação×produtividade** — quando dados de sustentação estão presentes, verificar se pessoas com alta carga de tickets (>= 3) também apresentam velocity baixa ou WIP alto. Correlacionar explicitamente: "Fulano carrega X tickets de suporte e está com Y tasks ativas — possível pressão de contexto."
```

**Parte B — DailyReportGenerator.ts:**

1. Adicionar import no topo: `import { fetchSustentacaoForReport } from './SupportBoardClient'`
   Também importar o tipo: `import type { SupportBoardSnapshot } from '../../renderer/src/types/ipc'`

2. No método `generate()`, após o step de baseline (progress 50%), adicionar fetch de sustentação:
```typescript
// 2.6 Fetch sustentação (graceful — null se não configurado)
progress('sustentacao', 'Buscando dados de sustentação…', 55)
let sustentacaoSnapshot: SupportBoardSnapshot | null = null
try {
  sustentacaoSnapshot = await fetchSustentacaoForReport(settings)
} catch (err) {
  log.warn('sustentação: falhou (graceful)', { error: err instanceof Error ? err.message : String(err) })
}
```

3. Passar `sustentacaoSnapshot` para `buildReport()` — estender a assinatura do método privado:
```typescript
private buildReport(
  personReports: PersonDailyData[],
  sprintOverview: SprintOverview | null,
  today: string,
  allActiveTasks?: TaskCycleInfo[],
  doneTasksPhaseTime?: Map<string, number[]>,
  sustentacao?: SupportBoardSnapshot | null,   // NOVO parâmetro opcional
): { content: string; analysisInput: { sprintOverview: string; perPersonSummary: string; alerts: string; pipelineHealth?: string; sustentacao?: string } }
```

4. Atualizar a chamada existente: `this.buildReport(personReports, sprintOverview, today, allActiveTasks, doneTasksPhaseTime, sustentacaoSnapshot)`

5. Dentro de `buildReport()`, após a seção Pipeline Health, adicionar seção Sustentação quando snapshot não-null:

```typescript
// ── Sustentação ──────────────────────────────────────────
let sustentacaoText = ''
if (sustentacao) {
  const lines_sust: string[] = []
  lines_sust.push('## Sustentação', '')
  lines_sust.push(`> Atualizado em: ${sustentacao.atualizadoEm.slice(0, 10)} | Abertos: ${sustentacao.ticketsAbertos} | Breach: ${sustentacao.ticketsEmBreach.length}`, '')

  if (sustentacao.complianceRate7d !== null) {
    lines_sust.push(`> SLA compliance 7d: **${sustentacao.complianceRate7d}%**${sustentacao.complianceRate30d !== null ? ` | 30d: **${sustentacao.complianceRate30d}%**` : ''}`, '')
  }

  // Cruzar porAssignee com pessoas do time
  const assigneeEntries = Object.entries(sustentacao.porAssignee)
    .filter(([, count]) => count > 0)
    .sort((a, b) => b[1] - a[1])

  if (assigneeEntries.length > 0) {
    lines_sust.push('### Carga por pessoa', '')
    for (const [slug, count] of assigneeEntries) {
      // Encontrar nome da pessoa pelo slug (personReports já carregados)
      const person = personReports.find(p => p.slug === slug)
      const nome = person?.nome ?? slug
      const alert = count >= 5 ? ' ⚠️' : count >= 3 ? ' 🔵' : ''
      lines_sust.push(`- **${nome}**: ${count} ticket(s) aberto(s)${alert}`)
    }
    lines_sust.push('')
  }

  if (sustentacao.ticketsEmBreach.length > 0) {
    lines_sust.push('### Tickets em Breach de SLA', '')
    for (const ticket of sustentacao.ticketsEmBreach.slice(0, 5)) {
      const assignee = ticket.assignee ?? 'sem assignee'
      lines_sust.push(`- **${ticket.key}** — ${ticket.summary} (${ticket.ageDias}d, ${assignee})`)
    }
    if (sustentacao.ticketsEmBreach.length > 5) {
      lines_sust.push(`- _...e mais ${sustentacao.ticketsEmBreach.length - 5} tickets_`)
    }
    lines_sust.push('')
  }

  lines.push(...lines_sust)

  // Texto resumido para o analysisInput da IA
  sustentacaoText = [
    `Board: ${sustentacao.ticketsAbertos} abertos, ${sustentacao.ticketsEmBreach.length} em breach`,
    sustentacao.complianceRate7d !== null ? `SLA compliance 7d: ${sustentacao.complianceRate7d}%` : '',
    assigneeEntries.length > 0
      ? 'Carga por pessoa: ' + assigneeEntries.map(([slug, n]) => {
          const nome = personReports.find(p => p.slug === slug)?.nome ?? slug
          return `${nome} (${n} tickets)`
        }).join(', ')
      : '',
  ].filter(Boolean).join('\n')
}
```

6. Incluir `sustentacao: sustentacaoText || undefined` no objeto `analysisInput` retornado.

7. Atualizar a chamada a `runHaikuAnalysis` — o parâmetro já inclui `sustentacao?` via interface estendida, nenhuma mudança na chamada necessária.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - DailyReportGenerator.ts compila sem erros
    - buildReport aceita parâmetro sustentacao opcional
    - analysisInput inclui campo sustentacao quando snapshot disponível
    - Prompt buildDailyAnalysisPrompt inclui seção sustentação e instrução de cruzamento
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passa sem erros nos arquivos modificados
2. `grep -n "fetchSustentacaoForReport" src/main/external/SupportBoardClient.ts` — encontra a função exportada
3. `grep -n "## Sustentação" src/main/external/DailyReportGenerator.ts` — encontra a seção no buildReport
4. `grep -n "sustentacao" src/main/prompts/daily-analysis.prompt.ts` — encontra o campo e a instrução de cruzamento
5. `grep -n "fetchSustentacaoForReport" src/main/external/DailyReportGenerator.ts` — confirma o import e chamada
</verification>

<success_criteria>
- fetchSustentacaoForReport() exportada e retorna null quando não configurado
- Daily Report inclui seção Sustentação quando board configurado
- Seção cruza porAssignee com nomes reais das pessoas do time
- analysisInput da IA inclui resumo de sustentação
- Prompt instrui IA a correlacionar carga de suporte com produtividade
- Compila sem erros TypeScript
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.12-sustentacao-reports-cruzamento-produtividade/999.12-01-SUMMARY.md`
</output>
