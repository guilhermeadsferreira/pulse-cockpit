---
phase: 04-action-system-ux-avancado
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/main/ingestion/ArtifactWriter.ts
  - src/main/ingestion/IngestionPipeline.ts
autonomous: true
requirements:
  - ACTN-05
must_haves:
  truths:
    - "O gestor ve evidencias acumuladas de multiplos 1:1s para cada objetivo PDI"
    - "Multiplos artefatos acumulam evidencias para o mesmo objetivo PDI no config.yaml da pessoa"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "PDIItem com campo evidencias: string[]"
      contains: "evidencias"
    - path: "src/main/ingestion/IngestionPipeline.ts"
      provides: "Logica de acumular evidencias PDI a partir de resultados do deep pass"
      contains: "evidencias"
  key_links:
    - from: "src/main/ingestion/IngestionPipeline.ts"
      to: "PersonConfig.pdi"
      via: "Adiciona evidencias ao PDI quando pdi_update.progresso_observado presente"
      pattern: "evidencias"
---

<objective>
Implementar evidence aggregation para PDI: multiplos artefatos acumulam evidencias no campo `evidencias: string[]` de cada objetivo PDI.

Purpose: O gestor ve de forma cumulativa todas as evidencias de progresso (ou falta de) para cada objetivo de desenvolvimento, vindas de diferentes 1:1s e cerimonias.
Output: PDIItem com campo evidencias, pipeline acumula evidencias a cada ingestao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/renderer/src/types/ipc.ts
@src/main/ingestion/IngestionPipeline.ts
@src/main/registry/PersonRegistry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo evidencias ao PDIItem e acumular no pipeline</name>
  <files>src/renderer/src/types/ipc.ts, src/main/ingestion/IngestionPipeline.ts</files>
  <read_first>
    - src/renderer/src/types/ipc.ts
    - src/main/ingestion/IngestionPipeline.ts
    - src/main/prompts/1on1-deep.prompt.ts
  </read_first>
  <action>
### Parte A — Type update

1. Em `src/renderer/src/types/ipc.ts`, na interface `PDIItem`, adicionar campo opcional:
```typescript
export interface PDIItem {
  objetivo: string
  status:   PDIStatus
  prazo?:   string
  evidencias?: string[]   // Evidencias cumulativas de multiplos artefatos
}
```

### Parte B — Acumular evidencias no pipeline

2. Em `src/main/ingestion/IngestionPipeline.ts`, localizar o bloco que processa `oneOnOneResult.pdi_update` (por volta da linha 670-710, onde ha `pdiChanged`).

No trecho que itera `oneOnOneResult.pdi_update.objetivos_mencionados`, APOS o bloco de `match.status = 'em_andamento'`, adicionar acumulacao de evidencia:

```typescript
// Acumular evidencia para objetivo mencionado
if (match) {
  const evidenciaTexto = oneOnOneResult.pdi_update.progresso_observado
    ? `[${date}] ${oneOnOneResult.pdi_update.progresso_observado}`
    : `[${date}] Mencionado em 1:1`
  
  if (!match.evidencias) match.evidencias = []
  // Evitar duplicata exata
  if (!match.evidencias.includes(evidenciaTexto)) {
    match.evidencias.push(evidenciaTexto)
    pdiChanged = true
  }
}
```

3. Quando `novo_objetivo_sugerido` cria novo PDI item, inicializar com evidencias:
No bloco `pdi.push({...})` que ja existe, adicionar:
```typescript
pdi.push({
  objetivo: oneOnOneResult.pdi_update.novo_objetivo_sugerido,
  status: 'nao_iniciado',
  evidencias: [`[${date}] Sugerido em 1:1 — ${oneOnOneResult.pdi_update.progresso_observado ?? 'objetivo identificado'}`],
})
```

4. Para ingestion de cerimonias (Pass Cerimonia), localizar onde `pontos_de_desenvolvimento` sao processados. Se existir logica de cruzamento com PDI, adicionar evidencia similar. Se nao existir, adicionar bloco APOS processamento de cerimonia:

```typescript
// Acumular evidencias PDI de cerimonias (se mencionam pontos de desenvolvimento)
if (aiResult.pontos_de_desenvolvimento?.length > 0) {
  try {
    const currentConfig = registry.get(slug)
    if (currentConfig?.pdi?.length > 0) {
      let pdiChanged = false
      for (const ponto of aiResult.pontos_de_desenvolvimento) {
        const pontoLower = ponto.toLowerCase()
        const match = currentConfig.pdi.find(p => {
          const objLower = p.objetivo.toLowerCase()
          return pontoLower.includes(objLower.split(' ').slice(0, 3).join(' '))
            || objLower.includes(pontoLower.split(' ').slice(0, 3).join(' '))
        })
        if (match) {
          if (!match.evidencias) match.evidencias = []
          const evidencia = `[${date}] Cerimonia: ${ponto.slice(0, 100)}`
          if (!match.evidencias.includes(evidencia)) {
            match.evidencias.push(evidencia)
            pdiChanged = true
          }
        }
      }
      if (pdiChanged) {
        registry.save({ ...currentConfig, pdi: currentConfig.pdi })
        this.log.info('PDI evidencias acumuladas via cerimonia', { slug })
      }
    }
  } catch (err) {
    this.log.warn('falha ao acumular evidencias PDI de cerimonia', { slug, error: err instanceof Error ? err.message : String(err) })
  }
}
```
NOTA: Este bloco so deve ser adicionado se o processamento de cerimonia ja tem acesso a `registry` (PersonRegistry) e `aiResult`. Verificar no read_first. Se nao tem acesso, omitir esta parte — o acumulo via 1:1 deep pass e suficiente para o ACTN-05.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ipc.ts PDIItem contains `evidencias?: string[]`
    - IngestionPipeline.ts contains `match.evidencias` in the pdi_update block
    - IngestionPipeline.ts contains `Mencionado em 1:1` or `progresso_observado`
    - IngestionPipeline.ts new pdi item push contains `evidencias:`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>PDIItem tem campo evidencias cumulativo. Pipeline acumula evidencias de 1:1s (e opcionalmente cerimonias). Evidencias nunca sobrescritas, so adicionadas.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- PDIItem type tem evidencias: string[]
- Pipeline acumula evidencias no PDI a cada ingestao de 1:1
- Novo objetivo PDI ja nasce com evidencia inicial
</verification>

<success_criteria>
1. Campo evidencias existe no PDIItem e e array cumulativo
2. Cada 1:1 com mencao a objetivo PDI adiciona evidencia datada
3. Evidencias nunca sobrescritas — so append
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-system-ux-avancado/04-03-SUMMARY.md`
</output>
