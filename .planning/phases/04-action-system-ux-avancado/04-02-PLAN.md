---
phase: 04-action-system-ux-avancado
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/main/registry/ActionRegistry.ts
  - src/main/prompts/1on1-deep.prompt.ts
  - src/main/ingestion/IngestionPipeline.ts
autonomous: true
requirements:
  - ACTN-02
  - ACTN-04
must_haves:
  truths:
    - "Acao vencida do gestor gera alerta de follow-up que aparece no TeamRiskPanel e na pauta"
    - "O deep pass do 1:1 atualiza a prioridade das acoes abertas quando novo contexto surge"
  artifacts:
    - path: "src/main/registry/ActionRegistry.ts"
      provides: "Metodo getEscalations para detectar acoes de gestor vencidas que bloqueiam liderado"
      contains: "getEscalations"
    - path: "src/main/prompts/1on1-deep.prompt.ts"
      provides: "Campo prioridade_atualizada no output do deep pass"
      contains: "prioridade_atualizada"
    - path: "src/main/ingestion/IngestionPipeline.ts"
      provides: "Logica de aplicar prioridade do deep pass no ActionRegistry"
      contains: "prioridade_atualizada"
  key_links:
    - from: "src/main/ingestion/IngestionPipeline.ts"
      to: "src/main/registry/ActionRegistry.ts"
      via: "Aplica prioridade inferida pelo deep pass via list+find+write pattern"
      pattern: "prioridade_atualizada"
    - from: "src/main/registry/ActionRegistry.ts"
      to: "src/main/index.ts"
      via: "getEscalations consumido pelo IPC handler 'actions:escalations' adicionado no Plan 04-04"
      pattern: "getEscalations"
    - from: "src/main/index.ts"
      to: "src/renderer/src/views/DashboardView.tsx"
      via: "IPC 'actions:escalations' consumido pelo TeamRiskPanel no Dashboard"
      pattern: "escalations"
---

<objective>
Implementar escalation de acoes do gestor vencidas e atualizacao automatica de prioridade pelo deep pass do 1:1.

Purpose: Garantir que promessas do gestor nao caiam no esquecimento e que a prioridade das acoes reflita o contexto mais recente.
Output: Metodo getEscalations no ActionRegistry, campo prioridade_atualizada no prompt 1:1 deep, aplicacao da prioridade no pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-action-system-ux-avancado/04-01-SUMMARY.md
@src/main/registry/ActionRegistry.ts
@src/main/prompts/1on1-deep.prompt.ts
@src/main/ingestion/IngestionPipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escalation de acoes do gestor e prioridade via deep pass</name>
  <files>src/main/registry/ActionRegistry.ts, src/main/prompts/1on1-deep.prompt.ts, src/main/ingestion/IngestionPipeline.ts</files>
  <read_first>
    - src/main/registry/ActionRegistry.ts
    - src/main/prompts/1on1-deep.prompt.ts
    - src/main/ingestion/IngestionPipeline.ts
    - src/renderer/src/types/ipc.ts
  </read_first>
  <action>
### Parte A — Escalation no ActionRegistry

1. Em `src/main/registry/ActionRegistry.ts`, adicionar metodo publico:

```typescript
/**
 * Retorna acoes do gestor vencidas que potencialmente bloqueiam acoes do liderado.
 * Usado pelo TeamRiskPanel e pela pauta para alertar escalation.
 */
getEscalations(slug: string, thresholdDays = 14): Array<{
  gestorAction: Action
  diasPendente: number
  relatedLideradoActions: Action[]
}> {
  const actions = this.list(slug)
  const today = Date.now()
  const escalations: Array<{
    gestorAction: Action
    diasPendente: number
    relatedLideradoActions: Action[]
  }> = []

  const gestorOpen = actions.filter(a => a.owner === 'gestor' && (a.status === 'open' || a.status === 'in_progress'))
  const lideradoOpen = actions.filter(a => a.owner === 'liderado' && (a.status === 'open' || a.status === 'in_progress'))

  for (const ga of gestorOpen) {
    const dias = Math.floor((today - new Date(ga.criadoEm).getTime()) / 86_400_000)
    if (dias < thresholdDays) continue

    // Buscar acoes do liderado que podem estar bloqueadas (mesma fonte ou tema similar)
    const related = lideradoOpen.filter(la => {
      if (ga.fonteArtefato && la.fonteArtefato && ga.fonteArtefato === la.fonteArtefato) return true
      // Substring match simples no texto
      const gaDesc = (ga.descricao ?? ga.texto).toLowerCase()
      const laDesc = (la.descricao ?? la.texto).toLowerCase()
      const gaWords = gaDesc.split(/\s+/).filter(w => w.length > 4)
      return gaWords.some(w => laDesc.includes(w))
    })

    escalations.push({ gestorAction: ga, diasPendente: dias, relatedLideradoActions: related })
  }

  return escalations
}
```

### Parte B — Prioridade no prompt 1:1 deep

2. Em `src/main/prompts/1on1-deep.prompt.ts`:

   a) Adicionar nova interface:
```typescript
export interface OneOnOnePrioridadeAtualizada {
  acao_id: string
  nova_prioridade: 'baixa' | 'media' | 'alta'
  motivo: string
}
```

   b) Na interface `OneOnOneResult`, adicionar campo:
```typescript
  prioridade_atualizada: OneOnOnePrioridadeAtualizada[]
```

   c) Na funcao `build1on1DeepPrompt`, no bloco de instrucoes JSON do prompt (onde define o schema de saida), adicionar campo ao schema:
```
"prioridade_atualizada": [
  {
    "acao_id": "id da acao existente",
    "nova_prioridade": "baixa | media | alta",
    "motivo": "razao para mudar prioridade baseada no contexto do 1:1"
  }
]
```
   E na descricao das instrucoes, adicionar paragrafo:
```
## Prioridade de acoes
Analise as acoes abertas fornecidas. Se o contexto do 1:1 revela urgencia nova (prazo apertado, bloqueio, pedido explicito) ou reduz urgencia (ja resolvido parcialmente, contexto mudou), inclua em prioridade_atualizada. Retorne array vazio se nenhuma mudanca necessaria.
```

### Parte C — Aplicacao da prioridade no pipeline

3. Em `src/main/ingestion/IngestionPipeline.ts`, no metodo que processa o resultado do 1on1 deep pass (procurar onde `oneOnOneResult` e processado, APOS o bloco de `updateFromFollowup` e `createFrom1on1Result`):

Adicionar bloco usando o padrao list+find+write do ActionRegistry (NAO usar `actionRegistry.save(action)` pois acoes carregadas via `list()` podem nao ter `personSlug` populado):
```typescript
// 6. Apply priority updates from deep pass
if (oneOnOneResult.prioridade_atualizada?.length > 0) {
  const currentActions = actionRegistry.list(slug)
  let prioChanged = false
  for (const prio of oneOnOneResult.prioridade_atualizada) {
    const action = currentActions.find(a => a.id === prio.acao_id)
    if (action && action.prioridade !== prio.nova_prioridade) {
      action.prioridade = prio.nova_prioridade
      prioChanged = true
      this.log.info('prioridade atualizada via deep pass', {
        slug,
        actionId: prio.acao_id,
        de: action.prioridade,
        para: prio.nova_prioridade,
        motivo: prio.motivo,
      })
    }
  }
  if (prioChanged) {
    actionRegistry.write(slug, currentActions)
  }
}
```

NOTA: Se `actionRegistry.write` for private, usar a alternativa: iterar cada acao com `actionRegistry.updateStatusWithSource` passando o status atual (nao muda status, mas grava). Ou usar pattern de `save()` adicionando `personSlug` ao action antes: `action.personSlug = slug; actionRegistry.save(action)`. Escolher a abordagem mais limpa apos ler o codigo.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ActionRegistry.ts contains `getEscalations(`
    - ActionRegistry.ts getEscalations returns array with gestorAction, diasPendente, relatedLideradoActions
    - 1on1-deep.prompt.ts contains `export interface OneOnOnePrioridadeAtualizada`
    - 1on1-deep.prompt.ts OneOnOneResult contains `prioridade_atualizada:`
    - 1on1-deep.prompt.ts build1on1DeepPrompt contains `prioridade_atualizada`
    - IngestionPipeline.ts contains `prioridade_atualizada` 
    - IngestionPipeline.ts contains `prioridade atualizada via deep pass`
    - IngestionPipeline.ts uses list+find+write or save-with-personSlug pattern (NOT bare `actionRegistry.save(action)` without setting personSlug)
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Acoes do gestor vencidas sao detectadas via getEscalations. Deep pass retorna prioridade_atualizada e pipeline aplica no ActionRegistry usando padrao list+find+write. Escalations acessiveis para consumo via IPC no Plan 04-04.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- ActionRegistry tem getEscalations que retorna acoes do gestor pendentes 14+ dias
- Deep pass 1:1 retorna prioridade_atualizada no resultado
- Pipeline aplica prioridade do deep pass nas acoes existentes usando padrao correto do ActionRegistry
</verification>

<success_criteria>
1. getEscalations detecta acoes do gestor vencidas e correlaciona com acoes do liderado
2. Prompt do deep pass solicita e recebe prioridade_atualizada
3. Pipeline aplica prioridade inferida sem regressao
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-system-ux-avancado/04-02-SUMMARY.md`
</output>
