---
phase: 04-action-system-ux-avancado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/types/ipc.ts
  - src/main/registry/ActionRegistry.ts
  - src/main/external/ExternalDataPass.ts
autonomous: true
requirements:
  - ACTN-03
  - ACTN-01
must_haves:
  truths:
    - "Toda mudanca de status de uma acao gera uma entrada em statusHistory com status, date e source"
    - "Uma issue fechada no Jira fecha automaticamente a acao correspondente no app sem intervencao manual"
    - "O auto-fechamento via Jira aparece no statusHistory com source='jira-sync'"
    - "Arquivos de acoes existentes sem statusHistory carregam normalmente sem erros"
  artifacts:
    - path: "src/renderer/src/types/ipc.ts"
      provides: "ActionStatusHistoryEntry type and updated Action interface"
      contains: "statusHistory"
    - path: "src/main/registry/ActionRegistry.ts"
      provides: "Audit trail recording on every status change + Jira sync method"
      contains: "statusHistory"
    - path: "src/main/external/ExternalDataPass.ts"
      provides: "Jira issue status check and auto-close trigger"
      contains: "syncActionsWithJira"
  key_links:
    - from: "src/main/external/ExternalDataPass.ts"
      to: "src/main/registry/ActionRegistry.ts"
      via: "syncActionsWithJira calls updateStatus with source='jira-sync'"
      pattern: "syncActionsWithJira"
    - from: "src/main/registry/ActionRegistry.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "Action type imports"
      pattern: "ActionStatusHistoryEntry"
---

<objective>
Adicionar audit trail (statusHistory[]) a todas as acoes e implementar sync bidirecional com Jira (auto-fechar acoes quando issue Done).

Purpose: Garantir rastreabilidade de mudancas de status e eliminar acoes fantasmas que ja foram resolvidas no Jira.
Output: Action type com statusHistory, ActionRegistry grava historico em toda mudanca, ExternalDataPass sincroniza com Jira.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/renderer/src/types/ipc.ts
@src/main/registry/ActionRegistry.ts
@src/main/external/ExternalDataPass.ts
@src/main/external/JiraClient.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar ActionStatusHistoryEntry type e statusHistory ao Action + audit trail no ActionRegistry</name>
  <files>src/renderer/src/types/ipc.ts, src/main/registry/ActionRegistry.ts</files>
  <read_first>
    - src/renderer/src/types/ipc.ts
    - src/main/registry/ActionRegistry.ts
  </read_first>
  <action>
1. Em `src/renderer/src/types/ipc.ts`, adicionar novo type ANTES da interface Action:

```typescript
export interface ActionStatusHistoryEntry {
  status: ActionStatus
  date: string        // YYYY-MM-DD
  source: 'manual' | 'ingestion' | '1on1-deep' | 'jira-sync' | 'escalation' | 'system'
}
```

2. Na interface `Action`, adicionar campo opcional:
```typescript
  statusHistory?: ActionStatusHistoryEntry[]
```

3. Em `src/main/registry/ActionRegistry.ts`:

   a) Importar `ActionStatusHistoryEntry` do ipc.ts (ja importa Action e ActionStatus de la).

   b) Criar metodo privado `appendHistory`:
```typescript
private appendHistory(action: Action, newStatus: ActionStatus, source: ActionStatusHistoryEntry['source']): void {
  if (!action.statusHistory) action.statusHistory = []
  action.statusHistory.push({
    status: newStatus,
    date: new Date().toISOString().slice(0, 10),
    source,
  })
}
```

   c) No metodo `updateStatus(slug, id, status)`, ANTES de `action.status = status`, chamar:
```typescript
this.appendHistory(action, status as ActionStatus, 'manual')
```

   d) No metodo `updateFromFollowup`, para cada status change (cumprida->done, em_andamento->in_progress, abandonada->cancelled, nao_mencionada), chamar:
```typescript
this.appendHistory(action, [novo_status], '1on1-deep')
```

   e) No metodo `createFromArtifact`, no objeto newActions, inicializar:
```typescript
statusHistory: [{ status: 'open' as ActionStatus, date, source: 'ingestion' as const }]
```

   f) No metodo `createFrom1on1Result`, no objeto newActions.push, inicializar:
```typescript
statusHistory: [{ status: 'open' as ActionStatus, date, source: '1on1-deep' as const }]
```

   g) Adicionar novo metodo publico `updateStatusWithSource`:
```typescript
updateStatusWithSource(slug: string, id: string, status: ActionStatus, source: ActionStatusHistoryEntry['source']): void {
  const actions = this.list(slug)
  let changed = false
  for (const action of actions) {
    if (action.id === id) {
      this.appendHistory(action, status, source)
      action.status = status
      if (status === 'done') action.concluidoEm = new Date().toISOString().slice(0, 10)
      changed = true
    }
  }
  if (changed) this.write(slug, actions)
}
```

   h) IMPORTANTE (backward-compat per CLAUDE.md): Todos os metodos que leem acoes (list, updateStatus, updateFromFollowup, etc.) ja funcionam com acoes existentes sem statusHistory porque o campo e opcional (`statusHistory?: ActionStatusHistoryEntry[]`). O metodo `appendHistory` inicializa o array se ausente (`if (!action.statusHistory) action.statusHistory = []`). Verificar que NENHUM codigo assume `action.statusHistory` como definido sem checagem — sempre usar optional chaining ou a checagem no appendHistory.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ipc.ts contains `export interface ActionStatusHistoryEntry`
    - ipc.ts Action interface contains `statusHistory?: ActionStatusHistoryEntry[]`
    - ActionRegistry.ts contains `private appendHistory(`
    - ActionRegistry.ts contains `updateStatusWithSource(`
    - ActionRegistry.ts updateStatus method contains `this.appendHistory(action,`
    - ActionRegistry.ts updateFromFollowup method contains `this.appendHistory(`
    - ActionRegistry.ts createFromArtifact contains `statusHistory:`
    - ActionRegistry.ts createFrom1on1Result contains `statusHistory:`
    - Existing action YAML files without statusHistory field load via list() without error
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Toda mudanca de status de acao gera entrada em statusHistory com status, date e source. Novas acoes ja nascem com statusHistory inicial. Arquivos existentes sem statusHistory carregam normalmente (campo opcional, appendHistory inicializa se ausente).</done>
</task>

<task type="auto">
  <name>Task 2: Implementar sync bidirecional Jira no ExternalDataPass</name>
  <files>src/main/external/ExternalDataPass.ts</files>
  <read_first>
    - src/main/external/ExternalDataPass.ts
    - src/main/external/JiraClient.ts
    - src/main/registry/ActionRegistry.ts
  </read_first>
  <action>
1. Ler `ExternalDataPass.ts` para entender o metodo `run(slug)` — ele ja instancia JiraClient e busca issues.

2. Adicionar metodo `syncActionsWithJira` ao ExternalDataPass:

```typescript
private async syncActionsWithJira(
  slug: string,
  jiraClient: JiraClient,
  actionRegistry: ActionRegistry,
): Promise<number> {
  const actions = actionRegistry.list(slug)
  const openActions = actions.filter(a => a.status === 'open' || a.status === 'in_progress')
  let closedCount = 0

  for (const action of openActions) {
    // Detectar referencia a issue Jira no texto ou descricao
    const jiraKeyRegex = /\b([A-Z][A-Z0-9]+-\d+)\b/
    const textToSearch = `${action.texto ?? ''} ${action.descricao ?? ''} ${action.fonteArtefato ?? ''}`
    const match = textToSearch.match(jiraKeyRegex)
    if (!match) continue

    const issueKey = match[1]
    try {
      // Buscar status da issue — usar searchIssuesByAssignee com JQL direto
      const issues = await jiraClient.searchIssuesByAssignee('', `key = "${issueKey}"`)
      if (issues.length === 0) continue

      const issue = issues[0]
      if (issue.statusCategory === 'done') {
        actionRegistry.updateStatusWithSource(slug, action.id, 'done', 'jira-sync')
        closedCount++
        log.info('acao auto-fechada via Jira sync', { slug, actionId: action.id, issueKey, issueStatus: issue.status })
      }
    } catch (err) {
      log.warn('falha ao verificar issue Jira para acao', { slug, actionId: action.id, issueKey, error: err instanceof Error ? err.message : String(err) })
    }
  }

  return closedCount
}
```

3. No metodo `run(slug)`, APOS a coleta de metricas Jira (apos o bloco `if (jiraClient)` que ja existe), adicionar chamada ao sync:

```typescript
// Sync actions with Jira — auto-close actions whose issues are Done
if (jiraClient) {
  try {
    const actionRegistry = new ActionRegistry(this.workspacePath)
    const closedCount = await this.syncActionsWithJira(slug, jiraClient, actionRegistry)
    if (closedCount > 0) {
      log.info('Jira sync: acoes auto-fechadas', { slug, closedCount })
    }
  } catch (err) {
    log.warn('Jira sync falhou (graceful)', { slug, error: err instanceof Error ? err.message : String(err) })
  }
}
```

4. Importar ActionRegistry no topo do arquivo se ainda nao importado:
```typescript
import { ActionRegistry } from '../registry/ActionRegistry'
```

5. Importar Logger se nao houver instancia de log:
```typescript
const log = Logger.getInstance().child('ExternalDataPass')
```
(verificar se ja existe — se sim, nao duplicar)
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ExternalDataPass.ts contains `private async syncActionsWithJira(`
    - ExternalDataPass.ts contains `import { ActionRegistry }`
    - ExternalDataPass.ts contains `updateStatusWithSource(slug, action.id, 'done', 'jira-sync')`
    - ExternalDataPass.ts run method contains `syncActionsWithJira`
    - ExternalDataPass.ts contains `jiraKeyRegex`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Issue fechada no Jira auto-fecha acao correspondente no app. Auto-fechamento registrado no statusHistory com source='jira-sync'. Falhas de sync sao graceful (nao interrompem o fluxo).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- Action type tem statusHistory opcional
- ActionRegistry grava historico em toda mudanca de status (manual, 1on1-deep, jira-sync)
- ExternalDataPass busca issues referenciadas nas acoes e auto-fecha quando Done
- Arquivos de acoes existentes sem statusHistory carregam sem erros
</verification>

<success_criteria>
1. Toda mudanca de status gera entrada em statusHistory
2. Issue fechada no Jira fecha automaticamente acao correspondente
3. Auto-fechamento aparece no audit trail com source='jira-sync'
4. Zero regressoes — TypeScript compila limpo
5. Backward-compat: acoes existentes sem statusHistory carregam e funcionam normalmente
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-system-ux-avancado/04-01-SUMMARY.md`
</output>
