---
phase: 04-action-system-ux-avancado
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/external/Scheduler.ts
  - src/main/index.ts
autonomous: true
requirements:
  - UX-03
must_haves:
  truths:
    - "A pauta de 1:1 e gerada automaticamente N dias antes do proximo encontro sem o gestor precisar acionar manualmente"
    - "O Scheduler verifica frequencia_1on1_dias e ultimo_1on1 para decidir quando gerar"
  artifacts:
    - path: "src/main/external/Scheduler.ts"
      provides: "Metodo checkAgendaGeneration que gera pautas automaticamente"
      contains: "checkAgendaGeneration"
    - path: "src/main/index.ts"
      provides: "Funcao generateAgendaForPerson extraida do handler IPC para reuso pelo Scheduler"
      contains: "generateAgendaForPerson"
  key_links:
    - from: "src/main/external/Scheduler.ts"
      to: "src/main/index.ts"
      via: "Scheduler importa e chama generateAgendaForPerson diretamente (sem IPC)"
      pattern: "generateAgendaForPerson"
---

<objective>
Implementar agenda generation agendada: Scheduler gera pauta automaticamente N dias antes do proximo 1:1 esperado.

Purpose: O gestor nao precisa lembrar de clicar "Gerar pauta" — o sistema antecipa baseado na frequencia configurada.
Output: Funcao generateAgendaForPerson extraida do handler IPC em index.ts, Scheduler com checkAgendaGeneration que roda no onAppStart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/external/Scheduler.ts
@src/main/index.ts
@src/main/registry/PersonRegistry.ts
@src/main/prompts/agenda.prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair generateAgendaForPerson do handler IPC e implementar checkAgendaGeneration no Scheduler</name>
  <files>src/main/index.ts, src/main/external/Scheduler.ts</files>
  <read_first>
    - src/main/index.ts
    - src/main/external/Scheduler.ts
    - src/main/registry/PersonRegistry.ts
    - src/main/prompts/agenda.prompt.ts
    - src/renderer/src/types/ipc.ts
  </read_first>
  <action>
### Parte A — Extrair funcao reutilizavel do handler IPC

1. Em `src/main/index.ts`, localizar o handler `ipcMain.handle('ai:generate-agenda', ...)`. Extrair a logica interna para uma funcao exportada ACIMA do handler:

```typescript
/**
 * Gera pauta de 1:1 para uma pessoa. Extraida do handler IPC para reuso pelo Scheduler.
 * Retorna o resultado da geracao ou throws em caso de erro.
 */
export async function generateAgendaForPerson(
  slug: string,
  workspacePath: string,
  claudeBinPath: string,
): Promise<{ success: boolean; pauta?: string }> {
  // ... mover a logica existente do handler aqui ...
  // Usar workspacePath e claudeBinPath como parametros em vez de ler settings internamente
}
```

2. Refatorar o handler IPC `'ai:generate-agenda'` para chamar a funcao extraida:

```typescript
ipcMain.handle('ai:generate-agenda', async (_event, slug: string) => {
  const settings = SettingsManager.load()
  if (!settings.claudeBinPath) {
    return { success: false, error: 'Claude CLI nao configurado' }
  }
  try {
    return await generateAgendaForPerson(slug, settings.workspacePath, settings.claudeBinPath)
  } catch (err) {
    return { success: false, error: err instanceof Error ? err.message : String(err) }
  }
})
```

IMPORTANTE: A logica extraida deve ser identica a existente no handler. Nao adicionar nem remover funcionalidade — apenas mover para funcao reutilizavel. Manter imports necessarios.

### Parte B — checkAgendaGeneration no Scheduler

3. Em `src/main/external/Scheduler.ts`, adicionar imports:

```typescript
import { generateAgendaForPerson } from '../index'
```

Verificar se ha risco de circular dependency (Scheduler importa de index.ts, index.ts pode importar Scheduler). Se houver, usar dynamic import: `const { generateAgendaForPerson } = await import('../index')`.

4. Adicionar constante:
```typescript
const AGENDA_DAYS_BEFORE = 2  // Gerar pauta 2 dias antes do proximo 1:1 esperado
```

5. Adicionar metodo publico `checkAgendaGeneration`:

```typescript
/**
 * Verifica se algum liderado precisa de pauta gerada automaticamente.
 * Criterio: proximo 1:1 esperado em <= AGENDA_DAYS_BEFORE dias e nao ha pauta recente.
 */
async checkAgendaGeneration(): Promise<{ generated: string[] }> {
  const settings = SettingsManager.load()
  if (!settings.claudeBinPath) {
    log.warn('auto agenda: Claude CLI nao configurado')
    return { generated: [] }
  }

  const registry = new PersonRegistry(settings.workspacePath)
  const people = registry.list().filter(p => p.relacao === 'liderado')
  const today = new Date()
  const generated: string[] = []

  for (const person of people) {
    try {
      // Calcular quando e o proximo 1:1 esperado
      const perfil = registry.getPerfil(person.slug)
      const ultimo1on1 = perfil?.frontmatter?.ultimo_1on1
      if (!ultimo1on1) continue  // Sem historico de 1:1 — nao gerar automaticamente

      const frequenciaDias = person.frequencia_1on1_dias ?? 14
      const ultimoDate = new Date(ultimo1on1)
      const proximoDate = new Date(ultimoDate.getTime() + frequenciaDias * 86_400_000)
      const diasAteProximo = Math.floor((proximoDate.getTime() - today.getTime()) / 86_400_000)

      // Gerar se proximo 1:1 e em <= AGENDA_DAYS_BEFORE dias
      if (diasAteProximo > AGENDA_DAYS_BEFORE) continue
      if (diasAteProximo < -3) continue  // Ja passou muito — nao gerar pauta atrasada

      // Verificar se ja existe pauta recente (ultimos 3 dias)
      const pautas = registry.listPautas(person.slug)
      const pautaRecente = pautas.find(p => {
        const diasSincePauta = Math.floor((today.getTime() - new Date(p.date).getTime()) / 86_400_000)
        return diasSincePauta <= 3
      })
      if (pautaRecente) continue  // Ja tem pauta recente — nao duplicar

      log.info('agenda auto-generation triggered', { slug: person.slug, diasAteProximo, frequenciaDias })

      // Gerar pauta chamando funcao extraida diretamente (sem IPC, sem BrowserWindow)
      const result = await generateAgendaForPerson(person.slug, settings.workspacePath, settings.claudeBinPath)
      if (result?.success) {
        generated.push(person.slug)
        log.info('pauta auto-gerada', { slug: person.slug })
      }
    } catch (err) {
      log.warn('auto agenda generation falhou', { slug: person.slug, error: err instanceof Error ? err.message : String(err) })
    }
  }

  return { generated }
}
```

### Parte C — Integrar no onAppStart

6. No metodo `onAppStart()` do Scheduler, APOS o bloco de daily report, adicionar:

```typescript
// Auto-generate agendas for upcoming 1:1s
try {
  const { generated } = await this.checkAgendaGeneration()
  if (generated.length > 0) {
    log.info('pautas auto-geradas no startup', { count: generated.length, people: generated })
  }
} catch (err) {
  log.warn('auto agenda check falhou', { error: err instanceof Error ? err.message : String(err) })
}
```
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - index.ts contains `export async function generateAgendaForPerson(`
    - index.ts IPC handler 'ai:generate-agenda' calls `generateAgendaForPerson(`
    - Scheduler.ts contains `async checkAgendaGeneration(`
    - Scheduler.ts contains `AGENDA_DAYS_BEFORE`
    - Scheduler.ts contains `generateAgendaForPerson(` (NOT executeJavaScript)
    - Scheduler.ts does NOT contain `executeJavaScript`
    - Scheduler.ts onAppStart contains `checkAgendaGeneration`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Scheduler gera pautas automaticamente 2 dias antes do proximo 1:1 esperado via funcao extraida generateAgendaForPerson (chamada direta, sem IPC/BrowserWindow). Nao duplica se pauta recente existe. Roda no startup do app.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` compila sem erros
- generateAgendaForPerson exportada de index.ts e reutilizada pelo handler IPC
- Scheduler tem checkAgendaGeneration que roda no onAppStart
- Pauta e gerada quando proximo 1:1 em <= 2 dias e nao ha pauta recente
- Falhas sao graceful (nao interrompem startup)
- Nenhum uso de executeJavaScript ou BrowserWindow para gerar pauta
</verification>

<success_criteria>
1. generateAgendaForPerson extraida como funcao reutilizavel em index.ts
2. checkAgendaGeneration calcula proximo 1:1 baseado em ultimo_1on1 + frequencia_1on1_dias
3. Pauta gerada automaticamente 2 dias antes sem intervencao manual
4. Nao duplica pauta se ja existe uma nos ultimos 3 dias
5. Scheduler chama funcao diretamente, sem IPC ou executeJavaScript
</success_criteria>

<output>
After completion, create `.planning/phases/04-action-system-ux-avancado/04-05-SUMMARY.md`
</output>
