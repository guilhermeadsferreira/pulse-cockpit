---
phase: 999.3-ingestion-performance-hybrid-model
plan: "02"
subsystem: ingestion
tags: [openrouter, hybrid-model, ingestion-pipeline, conditional-routing, fallback]

# Dependency graph
requires:
  - phase: 999.3-01
    provides: runOpenRouterPrompt in ClaudeRunner.ts, useHybridModel/openRouterApiKey in AppSettings
provides:
  - Pass Cerimônia com rota condicional OpenRouter/Claude CLI no IngestionPipeline
  - Fallback automático para Claude CLI quando OpenRouter falha
affects:
  - 999.3-03 (SettingsView hybrid fields UI — UI para as configurações que este plano usa)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rota condicional com fallback: hybridActive guard + fallback para Claude CLI"
    - "Settings carregados por método privado antes do loop de batch (não passados como param)"

key-files:
  created: []
  modified:
    - src/main/ingestion/IngestionPipeline.ts

key-decisions:
  - "hybridActive calculado antes do loop de batch (não dentro de cada iteração) para evitar múltiplas chamadas ao SettingsManager"
  - "openRouterModel hardcoded como 'google/gemma-3-4b-it:free' no código (não configurável via settings nesta fase)"
  - "import('./ClaudeRunner').ClaudeRunnerResult usado como tipo inline para let result (sem import adicional)"

patterns-established:
  - "Fallback pattern: if (hybridActive) { try OpenRouter; if fail → warn + fallback } else { Claude CLI }"
  - "Log de fallback segue padrão existente: console.warn('[IngestionPipeline] OpenRouter fallback para ...')"

requirements-completed:
  - PERF-01

# Metrics
duration: 15min
completed: 2026-03-27
---

# Phase 999.3 Plan 02: Rota Condicional no Pass Cerimônia Summary

**Pass Cerimônia roteado condicionalmente para OpenRouter (Gemma 3 free) quando híbrido ativo, com fallback automático para Claude CLI em caso de falha**

## Performance

- **Duration:** 15 min
- **Started:** 2026-03-27T12:00:00Z
- **Completed:** 2026-03-27T12:15:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Import de `runOpenRouterPrompt` adicionado ao IngestionPipeline.ts
- `runCerimoniaSignalsForPeople` lê settings e calcula `hybridActive` antes do loop de batch
- Quando `hybridActive=true`: usa OpenRouter; em falha, loga warning e executa fallback para Claude CLI
- Comportamento padrão (Claude CLI) 100% preservado quando híbrido inativo
- TypeScript compila sem erros nos arquivos modificados

## Task Commits

1. **Task 1: Rota condicional no Pass Cerimônia** - `f12a247` (feat)

**Nota:** Commit `cd64d73` trouxe os pré-requisitos do plano 01 (cherry-pick das mudanças do ClaudeRunner e SettingsManager do worktree paralelo).

## Files Created/Modified

- `src/main/ingestion/IngestionPipeline.ts` - Rota condicional OpenRouter/Claude CLI no método `runCerimoniaSignalsForPeople`

## Decisions Made

- `hybridActive` calculado uma vez antes do loop de batch (não por iteração) para evitar overhead de I/O desnecessário
- Modelo OpenRouter hardcoded como `'google/gemma-3-4b-it:free'` — não configurável via settings nesta fase, conforme especificado no plano
- Tipo `ClaudeRunnerResult` referenciado via import inline (`import('./ClaudeRunner').ClaudeRunnerResult`) para manter o `let result` tipado sem import adicional no topo do arquivo

## Deviations from Plan

None — plano executado exatamente como escrito. A única diferença foi a necessidade de cherry-pick dos commits do plano 01 no worktree (agentes paralelos), o que é operacional e não constitui desvio de escopo.

## Issues Encountered

- **Worktree sem mudanças do plano 01:** Este agente roda em `worktree-agent-a7e496bf`, que estava baseado em `main` sem as mudanças do plano 01 (feitas por `worktree-agent-a1132136`). Solução: cherry-pick dos commits `dc4fbd5` e `7ae2b28` antes de implementar o plano 02.

## User Setup Required

None — nenhuma configuração externa necessária. As configurações de `openRouterApiKey` e `useHybridModel` são gerenciadas pela SettingsView (plano 03).

## Next Phase Readiness

- Pass Cerimônia híbrido pronto para uso assim que `openRouterApiKey` e `useHybridModel=true` estiverem em settings
- Plano 03 (SettingsView hybrid fields UI) pode agora implementar os campos de UI que expõem essas configurações ao usuário

---
*Phase: 999.3-ingestion-performance-hybrid-model*
*Completed: 2026-03-27*
