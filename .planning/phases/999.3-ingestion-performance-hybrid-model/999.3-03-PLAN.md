---
phase: 999.3-ingestion-performance-hybrid-model
plan: "03"
type: execute
wave: 2
depends_on:
  - "999.3-01"
files_modified:
  - src/renderer/src/views/SettingsView.tsx
  - src/renderer/src/types/global.d.ts
autonomous: false
requirements:
  - PERF-01
must_haves:
  truths:
    - "SettingsView exibe campo de texto para colar a OpenRouter API key"
    - "SettingsView exibe toggle checkbox 'Usar modelo híbrido (OpenRouter)'"
    - "Toggle só é interativo quando openRouterApiKey está preenchida"
    - "Salvar settings persiste ambos os campos via IPC settings:save"
    - "Ao abrir Settings, os campos carregam com os valores salvos em disco"
  artifacts:
    - path: "src/renderer/src/views/SettingsView.tsx"
      provides: "Campo de key e toggle de modelo híbrido"
      contains: "openRouterApiKey"
    - path: "src/renderer/src/types/global.d.ts"
      provides: "Tipo AppSettings atualizado no window.api"
      contains: "openRouterApiKey"
  key_links:
    - from: "src/renderer/src/views/SettingsView.tsx"
      to: "src/preload/index.ts"
      via: "window.api.settings.save(form) com campos novos"
      pattern: "openRouterApiKey"
    - from: "src/renderer/src/types/global.d.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "import AppSettings"
      pattern: "AppSettings"
---

<objective>
Expor os campos do modelo híbrido na SettingsView: campo de API key e toggle de ativação.

Purpose: Sem a UI, o usuário não tem como ativar o híbrido. A key e o toggle precisam ser visíveis, editáveis e persistidos via o mesmo fluxo de save já existente.

Output:
- Nova seção "Modelo Híbrido (OpenRouter)" na SettingsView com campo de key + toggle
- `global.d.ts` verificado — herda AppSettings de ipc.ts que já tem os campos novos
- O preload não precisa de mudança — `settings:save` já aceita qualquer AppSettings

Checkpoint: verificação visual que o campo e toggle aparecem na Settings e que save/load funciona.
</objective>

<execution_context>
@/Users/guilhermeaugusto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guilhermeaugusto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-CONTEXT.md
@.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos do plano 01 que este plano consome -->

De src/renderer/src/types/ipc.ts (AppSettings atualizado no plano 01):
```typescript
export interface AppSettings {
  workspacePath: string
  claudeBinPath: string
  managerName?: string
  managerRole?: string
  ingestionModel?: string
  openRouterApiKey?: string   // novo
  useHybridModel?: boolean    // novo
}
```

Padrões existentes na SettingsView a seguir:
- `Section` component: `<Section icon={<Cpu size={14} />} title="..." desc="...">`
- `Field` component: `<Field label="..." hint="...">`
- `set(field: keyof AppSettings, value: unknown)` — função existente para atualizar form state
- Estilo de input: `styles.input`
- Estilo de botão secundário: `styles.btnSecondary`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seção híbrida na SettingsView + verificar global.d.ts</name>
  <files>
    src/renderer/src/views/SettingsView.tsx
    src/renderer/src/types/global.d.ts
  </files>
  <read_first>
    - src/renderer/src/views/SettingsView.tsx — ler completo; entender onde fica a seção "Claude Code CLI" para adicionar a nova seção logo antes dela; entender o padrão de Section/Field/input; verificar import de lucide-react
    - src/renderer/src/types/global.d.ts — verificar se AppSettings é importado de './ipc' (linha 1) e se não há redeclaração local dos campos — se importado, nenhuma mudança necessária exceto confirmar
    - src/renderer/src/types/ipc.ts — confirmar que openRouterApiKey e useHybridModel já estão no AppSettings (adicionados no plano 01)
  </read_first>
  <action>
**Passo 1 — global.d.ts:**

Verificar linha 1: `import type { AppSettings, ... } from './ipc'`. Como `AppSettings` em `ipc.ts` já tem `openRouterApiKey` e `useHybridModel` (adicionados no plano 01), o renderer herda automaticamente. Se não houver redeclaração local dos campos, o arquivo não precisa de mudança. Se existir redeclaração, adicionar os dois campos. Confirmar com grep — não alterar se não for necessário.

**Passo 2 — SettingsView.tsx — importar Zap:**

Na linha do import de lucide-react (linha 2), adicionar `Zap` ao conjunto existente:
```typescript
import { FolderOpen, Cpu, CheckCircle2, XCircle, User, RefreshCw, Zap } from 'lucide-react'
```

**Passo 3 — SettingsView.tsx — adicionar nova seção:**

Localizar a `<Section>` com `title="Claude Code CLI"`. Inserir ANTES dela (após o fechamento `</Section>` da seção "Dados") a nova seção:

```tsx
{/* Modelo Híbrido */}
<Section
  icon={<Zap size={14} />}
  title="Modelo Híbrido (OpenRouter)"
  desc="Usa modelos leves via OpenRouter para passes de baixa complexidade — reduz latência de ingestão"
>
  <Field
    label="OpenRouter API Key"
    hint="Cole sua key de api.openrouter.ai/keys — armazenada localmente em ~/.pulsecockpit/settings.json"
  >
    <input
      style={styles.input}
      type="password"
      value={form.openRouterApiKey ?? ''}
      onChange={(e) => set('openRouterApiKey', e.target.value || undefined)}
      placeholder="sk-or-v1-..."
    />
  </Field>
  <Field
    label="Usar modelo híbrido"
    hint={!form.openRouterApiKey ? 'Configure a API Key acima para ativar' : 'Ativo: Pass Cerimônia usará OpenRouter com fallback para Claude CLI'}
  >
    <label style={{ display: 'flex', alignItems: 'center', gap: 8, cursor: form.openRouterApiKey ? 'pointer' : 'default' }}>
      <input
        type="checkbox"
        checked={form.useHybridModel ?? false}
        disabled={!form.openRouterApiKey}
        onChange={(e) => set('useHybridModel', e.target.checked)}
      />
      <span style={{ fontSize: 12, color: form.openRouterApiKey ? 'var(--text-primary)' : 'var(--text-muted)' }}>
        Ativar modelo híbrido
      </span>
    </label>
  </Field>
</Section>
```

Não alterar nenhuma outra seção ou lógica existente.
  </action>
  <verify>
    <automated>grep -n "openRouterApiKey\|useHybridModel\|Modelo Híbrido\|OpenRouter\|Zap" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/SettingsView.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SettingsView.tsx contém `openRouterApiKey`
    - SettingsView.tsx contém `useHybridModel`
    - SettingsView.tsx contém `Modelo Híbrido`
    - SettingsView.tsx contém `Zap` no import de lucide-react
    - SettingsView.tsx contém `type="password"` no input da API key
    - SettingsView.tsx contém `disabled={!form.openRouterApiKey}` no checkbox
    - TypeScript compila sem erros
  </acceptance_criteria>
  <done>SettingsView tem campo de key + toggle de modelo híbrido. Toggle desabilitado quando key ausente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verificação visual dos campos híbridos na SettingsView</name>
  <files>N/A — verificação visual, sem alteração de código</files>
  <action>
Nenhuma ação de código neste passo. O executor deve guiar o usuário a verificar a UI e o arquivo de settings conforme as instruções em how-to-verify.
  </action>
  <what-built>
    Campo "OpenRouter API Key" (type=password) e toggle "Ativar modelo híbrido" na SettingsView, dentro de uma nova seção "Modelo Híbrido (OpenRouter)" posicionada antes da seção "Claude Code CLI".
  </what-built>
  <how-to-verify>
    1. Abrir o app (npm run dev ou app já aberto)
    2. Navegar para a tela de Settings
    3. Verificar que a seção "Modelo Híbrido (OpenRouter)" aparece antes da seção "Claude Code CLI"
    4. Verificar que o campo de API key aceita input (texto mascarado como password)
    5. Verificar que o toggle "Ativar modelo híbrido" está DESABILITADO quando o campo de key está vazio
    6. Digitar qualquer texto no campo de key → verificar que o toggle fica HABILITADO
    7. Marcar o toggle e clicar "Salvar alterações" → abrir ~/.pulsecockpit/settings.json e confirmar que os campos `openRouterApiKey` e `useHybridModel` estão salvos
    8. Fechar e reabrir o app → Settings deve carregar com os valores salvos
  </how-to-verify>
  <resume-signal>Digite "aprovado" se tudo funciona conforme descrito, ou descreva o problema encontrado.</resume-signal>
  <verify>
    <automated>cat ~/.pulsecockpit/settings.json | grep -E "openRouterApiKey|useHybridModel"</automated>
  </verify>
  <done>Gestor aprovou a UI e confirmou que save/load persiste os campos corretamente.</done>
</task>

</tasks>

<verification>
```bash
grep -n "openRouterApiKey\|useHybridModel\|Modelo Híbrido\|Zap" \
  /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/views/SettingsView.tsx

cat ~/.pulsecockpit/settings.json
```
</verification>

<success_criteria>
- Seção "Modelo Híbrido (OpenRouter)" visível na SettingsView
- Campo de API key aceita input mascarado
- Toggle desabilitado sem key, habilitado com key
- Salvar persiste ambos os campos em ~/.pulsecockpit/settings.json
- Aprovado pelo gestor no checkpoint visual
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-03-SUMMARY.md`
</output>
