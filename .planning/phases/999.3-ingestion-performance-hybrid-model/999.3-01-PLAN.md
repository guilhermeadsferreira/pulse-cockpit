---
phase: 999.3-ingestion-performance-hybrid-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/ingestion/ClaudeRunner.ts
  - src/main/registry/SettingsManager.ts
  - src/renderer/src/types/ipc.ts
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "AppSettings aceita os campos openRouterApiKey e useHybridModel sem quebrar settings existentes"
    - "ClaudeRunner exporta runOpenRouterPrompt com mesma assinatura de retorno que runClaudePrompt"
    - "runOpenRouterPrompt chama api.openrouter.ai/api/v1/chat/completions via fetch e retorna ClaudeRunnerResult"
    - "Fallback para Claude CLI ocorre automaticamente quando chamada OpenRouter falha"
  artifacts:
    - path: "src/main/ingestion/ClaudeRunner.ts"
      provides: "runOpenRouterPrompt function"
      contains: "runOpenRouterPrompt"
    - path: "src/main/registry/SettingsManager.ts"
      provides: "AppSettings com openRouterApiKey e useHybridModel"
      contains: "openRouterApiKey"
    - path: "src/renderer/src/types/ipc.ts"
      provides: "AppSettings renderer type atualizado"
      contains: "openRouterApiKey"
  key_links:
    - from: "src/main/ingestion/ClaudeRunner.ts"
      to: "https://openrouter.ai/api/v1/chat/completions"
      via: "fetch com Authorization: Bearer"
      pattern: "openrouter\\.ai"
    - from: "src/main/registry/SettingsManager.ts"
      to: "src/renderer/src/types/ipc.ts"
      via: "campos em espelho no AppSettings"
      pattern: "openRouterApiKey"
---

<objective>
Estabelecer a fundação do modelo híbrido: adicionar os campos de settings necessários e criar a função `runOpenRouterPrompt` no ClaudeRunner.

Purpose: Outros planos desta fase dependem desta fundação. O IngestionPipeline não pode ser migrado e a SettingsView não pode exibir os campos até que os tipos e a função existam.

Output:
- `AppSettings` com `openRouterApiKey?: string` e `useHybridModel?: boolean` (em SettingsManager e em types/ipc.ts)
- `runOpenRouterPrompt(apiKey, model, prompt, timeoutMs)` exportada do ClaudeRunner — retorna `ClaudeRunnerResult`
- Sem quebras: settings existentes sem esses campos continuam funcionando via defaults
</objective>

<execution_context>
@/Users/guilhermeaugusto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guilhermeaugusto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender AppSettings com campos híbridos</name>
  <files>
    src/main/registry/SettingsManager.ts
    src/renderer/src/types/ipc.ts
  </files>
  <read_first>
    - src/main/registry/SettingsManager.ts — ver interface AppSettings atual e campo ingestionModel como precedente
    - src/renderer/src/types/ipc.ts — ver AppSettings espelhado no renderer (deve ser idêntico ao do main)
  </read_first>
  <action>
Em `src/main/registry/SettingsManager.ts`, adicionar dois campos opcionais à interface `AppSettings` logo após o campo `ingestionModel`:

```typescript
/** API key do OpenRouter para modelo híbrido. Armazenada em plaintext (uso pessoal). */
openRouterApiKey?: string
/** Ativar modelo híbrido (OpenRouter para passes elegíveis). Só tem efeito se openRouterApiKey presente. */
useHybridModel?: boolean
```

Não alterar os DEFAULTS — campos opcionais sem default são undefined, o que é comportamento correto.

Em `src/renderer/src/types/ipc.ts`, espelhar os mesmos dois campos na interface `AppSettings` do renderer (que começa na linha 4), mantendo os mesmos comentários JSDoc.

Não alterar mais nada nos dois arquivos.
  </action>
  <verify>
    <automated>grep -n "openRouterApiKey" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/registry/SettingsManager.ts /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/types/ipc.ts</automated>
  </verify>
  <acceptance_criteria>
    - SettingsManager.ts contém `openRouterApiKey?: string`
    - SettingsManager.ts contém `useHybridModel?: boolean`
    - types/ipc.ts contém `openRouterApiKey?: string`
    - types/ipc.ts contém `useHybridModel?: boolean`
    - Nenhum campo existente foi removido ou renomeado em nenhum dos dois arquivos
    - `npx tsc --noEmit` (ou equivalente do projeto) não reporta erros nos dois arquivos
  </acceptance_criteria>
  <done>AppSettings aceita os dois novos campos opcionais em ambas as camadas sem quebrar settings carregados de disco sem esses campos.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar runOpenRouterPrompt no ClaudeRunner</name>
  <files>
    src/main/ingestion/ClaudeRunner.ts
  </files>
  <read_first>
    - src/main/ingestion/ClaudeRunner.ts — ler o arquivo completo; entender interface ClaudeRunnerResult, função runClaudePrompt, função parseOutput e o padrão de log com ts()
  </read_first>
  <action>
Adicionar a função `runOpenRouterPrompt` ao final de `src/main/ingestion/ClaudeRunner.ts`, após a função `parseOutput` existente.

A função deve:
1. Aceitar os parâmetros: `apiKey: string`, `model: string`, `prompt: string`, `timeoutMs = 60_000`
2. Retornar `Promise<ClaudeRunnerResult>` — mesma interface de retorno que `runClaudePrompt`
3. Fazer um fetch POST para `https://openrouter.ai/api/v1/chat/completions` com:
   - Header `Authorization: Bearer ${apiKey}`
   - Header `Content-Type: application/json`
   - Header `HTTP-Referer: pulse-cockpit` (recomendado pelo OpenRouter)
   - Body: `{ model, messages: [{ role: 'user', content: prompt }], temperature: 0 }`
4. Aplicar timeout via `AbortController` + `setTimeout(controller.abort, timeoutMs)`
5. Em caso de sucesso HTTP (status 2xx): extrair `choices[0].message.content`, passar por `parseOutput` e retornar
6. Em caso de falha HTTP: retornar `{ success: false, error: \`OpenRouter HTTP ${status}: ${body.error?.message ?? status}\` }`
7. Em caso de erro de rede ou timeout: retornar `{ success: false, error: err.message }`
8. Logar entrada e saída com o mesmo padrão de `ts()` já usado no arquivo: `[OpenRouter] ${ts()} model=${model} (${bytes} bytes)` ao chamar e `[OpenRouter] ${ts()} status=${status} elapsed=${elapsed}s` ao retornar

**Não modificar** nenhuma função existente. Apenas adicionar a nova função e exportá-la.

Assinatura final a exportar:
```typescript
export async function runOpenRouterPrompt(
  apiKey: string,
  model: string,
  prompt: string,
  timeoutMs = 60_000,
): Promise<ClaudeRunnerResult>
```

A função `parseOutput` já existe no arquivo e pode ser chamada diretamente (não é exportada mas está no mesmo módulo).
  </action>
  <verify>
    <automated>grep -n "runOpenRouterPrompt\|openrouter\.ai\|AbortController" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/ingestion/ClaudeRunner.ts</automated>
  </verify>
  <acceptance_criteria>
    - ClaudeRunner.ts contém `export async function runOpenRouterPrompt(`
    - ClaudeRunner.ts contém `https://openrouter.ai/api/v1/chat/completions`
    - ClaudeRunner.ts contém `AbortController`
    - ClaudeRunner.ts contém `HTTP-Referer`
    - ClaudeRunner.ts contém `choices[0]` ou `choices\[0\]` para extrair o conteúdo da resposta
    - Funções existentes `runClaudePrompt`, `attemptRun`, `spawnOnce`, `parseOutput` permanecem inalteradas (verificar com git diff)
    - TypeScript compila sem erros (build local ou tsc --noEmit)
  </acceptance_criteria>
  <done>runOpenRouterPrompt está exportada, faz chamada REST para OpenRouter, aplica timeout, parseia o JSON de resposta com parseOutput existente e retorna ClaudeRunnerResult.</done>
</task>

</tasks>

<verification>
```bash
grep -n "openRouterApiKey\|useHybridModel" \
  /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/registry/SettingsManager.ts \
  /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/renderer/src/types/ipc.ts

grep -n "runOpenRouterPrompt\|openrouter\.ai" \
  /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/ingestion/ClaudeRunner.ts
```
</verification>

<success_criteria>
- AppSettings tem `openRouterApiKey?: string` e `useHybridModel?: boolean` em SettingsManager e types/ipc.ts
- ClaudeRunner exporta `runOpenRouterPrompt` com chamada REST para OpenRouter
- TypeScript compila sem erros
- Nenhuma funcionalidade existente foi alterada
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-01-SUMMARY.md`
</output>
