---
phase: 999.3-ingestion-performance-hybrid-model
plan: "02"
type: execute
wave: 2
depends_on:
  - "999.3-01"
files_modified:
  - src/main/ingestion/IngestionPipeline.ts
autonomous: true
requirements:
  - PERF-01
must_haves:
  truths:
    - "Pass Cerimônia usa runOpenRouterPrompt quando useHybridModel=true e openRouterApiKey presente"
    - "Pass Cerimônia usa runClaudePrompt quando useHybridModel=false ou openRouterApiKey ausente (comportamento atual inalterado)"
    - "Se runOpenRouterPrompt falha, há fallback automático para runClaudePrompt com log de warning"
    - "O modelo OpenRouter usado é configurável via settings.ingestionModel com default 'google/gemma-3-4b-it:free'"
  artifacts:
    - path: "src/main/ingestion/IngestionPipeline.ts"
      provides: "Rota condicional no Pass Cerimônia"
      contains: "useHybridModel"
  key_links:
    - from: "src/main/ingestion/IngestionPipeline.ts"
      to: "src/main/ingestion/ClaudeRunner.ts"
      via: "import runOpenRouterPrompt"
      pattern: "runOpenRouterPrompt"
    - from: "src/main/ingestion/IngestionPipeline.ts"
      to: "src/main/registry/SettingsManager.ts"
      via: "settings.useHybridModel && settings.openRouterApiKey"
      pattern: "useHybridModel"
---

<objective>
Migrar o Pass Cerimônia no IngestionPipeline para usar rota condicional: OpenRouter quando híbrido ativo, Claude CLI como fallback/padrão.

Purpose: Esta é a mudança comportamental central da fase. Sem ela, a infraestrutura do plano 01 nunca é exercitada.

Output:
- `runCerimoniaSignalsForPeople` no IngestionPipeline usa `runOpenRouterPrompt` quando `settings.useHybridModel === true && settings.openRouterApiKey` é truthy
- Fallback automático para `runClaudePrompt` se OpenRouter falha, com log `[IngestionPipeline] OpenRouter fallback para Claude CLI: ${err}`
- Modelo OpenRouter padrão: `'google/gemma-3-4b-it:free'` (substituível via `settings.openRouterModel` se quiser, mas não é requisito desta fase — usar string literal no código como default)
</objective>

<execution_context>
@/Users/guilhermeaugusto/.claude/get-shit-done/workflows/execute-plan.md
@/Users/guilhermeaugusto/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-CONTEXT.md
@.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos criados no Plano 01 que este plano consome -->

De src/main/ingestion/ClaudeRunner.ts (adicionado no plano 01):
```typescript
export async function runOpenRouterPrompt(
  apiKey: string,
  model: string,
  prompt: string,
  timeoutMs?: number,  // default 60_000
): Promise<ClaudeRunnerResult>

// ClaudeRunnerResult já existia:
export interface ClaudeRunnerResult {
  success: boolean
  data?: unknown
  rawOutput?: string
  error?: string
}
```

De src/main/registry/SettingsManager.ts (adicionado no plano 01):
```typescript
export interface AppSettings {
  workspacePath: string
  claudeBinPath: string
  managerName?: string
  managerRole?: string
  ingestionModel?: string
  openRouterApiKey?: string   // novo
  useHybridModel?: boolean    // novo
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rota condicional no Pass Cerimônia</name>
  <files>
    src/main/ingestion/IngestionPipeline.ts
  </files>
  <read_first>
    - src/main/ingestion/IngestionPipeline.ts — ler especificamente o método `runCerimoniaSignalsForPeople` (linhas ~280-358) para entender o ponto de injeção exato; também ler o import de `runClaudePrompt` no topo do arquivo (linha 5)
    - src/main/ingestion/ClaudeRunner.ts — confirmar assinatura de `runOpenRouterPrompt` após plano 01
  </read_first>
  <action>
**Passo 1 — Adicionar import:**

No topo de `IngestionPipeline.ts`, na linha que já importa `runClaudePrompt` do `ClaudeRunner`, adicionar `runOpenRouterPrompt` ao mesmo import:

```typescript
import { runClaudePrompt, runOpenRouterPrompt } from './ClaudeRunner'
```

**Passo 2 — Modificar runCerimoniaSignalsForPeople:**

A assinatura do método já recebe `claudeBinPath: string` mas não recebe settings. Antes do loop interno `batch.map(async (slug) => {` (linha ~294), ler os settings:

```typescript
const settings = SettingsManager.load()
const hybridActive = !!(settings.useHybridModel && settings.openRouterApiKey)
const openRouterModel = 'google/gemma-3-4b-it:free'
```

**Passo 3 — Substituir a chamada `runClaudePrompt` dentro de `runCerimoniaSignalsForPeople`:**

Localizar a linha atual (aproximadamente linha 315):
```typescript
const result = await runClaudePrompt(claudeBinPath, prompt, 60_000)
```

Substituir por lógica condicional com fallback:

```typescript
let result: import('./ClaudeRunner').ClaudeRunnerResult

if (hybridActive) {
  result = await runOpenRouterPrompt(settings.openRouterApiKey!, openRouterModel, prompt, 60_000)
  if (!result.success) {
    console.warn(`[IngestionPipeline] OpenRouter falhou para "${slug}" (${result.error}), fallback para Claude CLI`)
    result = await runClaudePrompt(claudeBinPath, prompt, 60_000)
  }
} else {
  result = await runClaudePrompt(claudeBinPath, prompt, 60_000)
}
```

**Não alterar nada mais no arquivo.** O restante do método (validação de schema, lock de pessoa, ArtifactWriter, notifyRenderer) permanece idêntico.
  </action>
  <verify>
    <automated>grep -n "runOpenRouterPrompt\|hybridActive\|useHybridModel\|openRouterModel\|OpenRouter falhou" /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/ingestion/IngestionPipeline.ts</automated>
  </verify>
  <acceptance_criteria>
    - IngestionPipeline.ts contém `import { runClaudePrompt, runOpenRouterPrompt } from './ClaudeRunner'`
    - IngestionPipeline.ts contém `hybridActive`
    - IngestionPipeline.ts contém `google/gemma-3-4b-it:free`
    - IngestionPipeline.ts contém `OpenRouter falhou` (mensagem de warning do fallback)
    - IngestionPipeline.ts contém `useHybridModel`
    - A mudança está APENAS dentro do método `runCerimoniaSignalsForPeople` — nenhum outro método foi alterado
    - TypeScript compila sem erros
  </acceptance_criteria>
  <done>Pass Cerimônia usa OpenRouter quando híbrido ativo com fallback automático para Claude CLI.</done>
</task>

</tasks>

<verification>
```bash
grep -n "runOpenRouterPrompt\|hybridActive\|google/gemma-3-4b-it\|OpenRouter falhou\|useHybridModel" \
  /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit/src/main/ingestion/IngestionPipeline.ts
```
</verification>

<success_criteria>
- Pass Cerimônia usa rota condicional: OpenRouter quando `useHybridModel=true && openRouterApiKey` truthy
- Fallback automático para Claude CLI se OpenRouter falha, com log de warning
- Comportamento padrão (Claude CLI) preservado quando híbrido inativo
- TypeScript compila sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/999.3-ingestion-performance-hybrid-model/999.3-02-SUMMARY.md`
</output>
