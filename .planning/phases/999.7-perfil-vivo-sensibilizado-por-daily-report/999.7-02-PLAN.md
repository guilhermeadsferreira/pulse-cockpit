---
phase: 999.7-perfil-vivo-sensibilizado-por-daily-report
plan: 02
type: execute
wave: 2
depends_on: [999.7-01]
files_modified:
  - src/main/external/SprintReportGenerator.ts
  - src/main/external/MonthlyReportGenerator.ts
autonomous: true
requirements: [MTRW-05, MTRW-06]

must_haves:
  truths:
    - "O sprint report persiste resultado consolidado (SP entregues vs planejados, cycle time, entregas, bloqueios) no metricas.md"
    - "O monthly report persiste tendencia mensal (destaques, pontos de atencao, deltas vs mes anterior) no metricas.md"
    - "Entradas antigas sao descartadas automaticamente (6 sprints, 6 meses)"
    - "Sprint e monthly atualizam a secao Momento Atual com dados mais recentes"
  artifacts:
    - path: "src/main/external/SprintReportGenerator.ts"
      provides: "Persistencia de resultado de sprint no metricas.md"
      contains: "MetricsWriter"
    - path: "src/main/external/MonthlyReportGenerator.ts"
      provides: "Persistencia de tendencia mensal no metricas.md"
      contains: "MetricsWriter"
  key_links:
    - from: "src/main/external/SprintReportGenerator.ts"
      to: "src/main/external/MetricsWriter.ts"
      via: "import MetricsWriter, chamada no final de generate()"
      pattern: "MetricsWriter"
    - from: "src/main/external/MonthlyReportGenerator.ts"
      to: "src/main/external/MetricsWriter.ts"
      via: "import MetricsWriter, chamada no final de generate()"
      pattern: "MetricsWriter"
---

<objective>
Integrar MetricsWriter nos geradores Sprint e Monthly para persistir dados estruturados de sprint e tendencia mensal no metricas.md.

Purpose: Completar a cobertura das 4 cadencias de relatorio, garantindo que sprint results e monthly trends tambem alimentem o perfil quantitativo da pessoa.
Output: SprintReportGenerator e MonthlyReportGenerator persistindo no metricas.md apos gerar relatorio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.7-perfil-vivo-sensibilizado-por-daily-report/999.7-CONTEXT.md
@.planning/phases/999.7-perfil-vivo-sensibilizado-por-daily-report/999.7-01-SUMMARY.md

@src/main/external/MetricsWriter.ts
@src/main/external/SprintReportGenerator.ts
@src/main/external/MonthlyReportGenerator.ts

<interfaces>
<!-- Key types from MetricsWriter (created in Plan 01) -->

From src/main/external/MetricsWriter.ts:
```typescript
export interface SprintEntry {
  nome: string
  spEntregues: number
  spPlanejados: number
  cycleTimeMedio: number
  entregas: string[]
  bloqueios: string[]
}

export interface MonthlyEntry {
  mes: string
  destaques: string[]
  pontosAtencao: string[]
  deltasVsMesAnterior: Record<string, string>
}

export interface MomentoAtualEntry {
  velocity: string
  cycleTime: string
  codeReview: string
  alertasAtivos: number
}

export class MetricsWriter {
  constructor(workspacePath: string)
  writeSprint(slug: string, entry: SprintEntry): void
  writeMonthly(slug: string, entry: MonthlyEntry): void
  updateMomentoAtual(slug: string, momento: MomentoAtualEntry): void
}
```

From src/main/external/ExternalDataPass.ts:
```typescript
export interface ExternalDataSnapshot {
  jira: JiraPersonMetrics | null
  github: GitHubPersonMetrics | null
  insights: CrossInsight[]
  atualizadoEm: string
}
```

From src/main/external/SprintReportGenerator.ts:
```typescript
interface PersonSprintData {
  nome: string
  slug: string
  snapshot: ExternalDataSnapshot | null
  previousCommits30d?: number
  previousPrsMerged30d?: number
}
// generate() receives sprint: JiraSprint
```

From src/main/external/MonthlyReportGenerator.ts:
```typescript
interface PersonMonthlyData {
  nome: string
  slug: string
  snapshot: ExternalDataSnapshot | null
  monthlyGithub: MonthlyGitHubData
  previous: MonthlyPreviousData
  narrativeContext: string
  baseline: { avgCommits: number; avgPRsMerged: number; avgReviews: number } | null
}
```

From src/main/external/JiraMetrics.ts:
```typescript
export interface SprintSummary {
  nome: string
  inicio: string
  fim: string
  comprometido: number
  entregue: number
  totalIssues: number
  issuesConcluidas: number
}

export interface JiraPersonMetrics {
  storyPointsSprint: number
  tempoMedioCicloDias: number
  blockersAtivos: Blocker[]
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrar MetricsWriter no SprintReportGenerator</name>
  <files>src/main/external/SprintReportGenerator.ts</files>
  <read_first>
    - src/main/external/MetricsWriter.ts (verificar exports SprintEntry, MomentoAtualEntry e assinaturas)
    - src/main/external/SprintReportGenerator.ts (entender generate() flow completo, PersonSprintData, como buildReport usa snapshot)
    - .planning/phases/999.7-perfil-vivo-sensibilizado-por-daily-report/999.7-CONTEXT.md (decisoes D-03, D-05, D-12)
  </read_first>
  <action>
**SprintReportGenerator.ts** (per D-03, D-12):

1. Adicionar import no topo:
   ```typescript
   import { MetricsWriter, type SprintEntry, type MomentoAtualEntry } from './MetricsWriter'
   ```

2. No metodo `generate()`, APOS a linha `writeFileSync(filePath, content, 'utf-8')` (linha ~86) e ANTES do `return filePath`:
   ```typescript
   // Persistir metricas no metricas.md (per D-03, D-12)
   const metricsWriter = new MetricsWriter(this.workspacePath)
   for (const person of personData) {
     try {
       const jira = person.snapshot?.jira
       const github = person.snapshot?.github
       const sprintSummary = jira?.sprintAtual

       // Extrair entregas e bloqueios do snapshot
       const entregas: string[] = []
       const bloqueios: string[] = []

       if (jira?.blockersAtivos) {
         for (const b of jira.blockersAtivos) {
           bloqueios.push(`${b.key}: ${b.summary}`)
         }
       }

       const sprintEntry: SprintEntry = {
         nome: sprint.name,
         spEntregues: sprintSummary?.entregue ?? 0,
         spPlanejados: sprintSummary?.comprometido ?? 0,
         cycleTimeMedio: jira?.tempoMedioCicloDias ?? 0,
         entregas,
         bloqueios,
       }
       metricsWriter.writeSprint(person.slug, sprintEntry)

       // Atualizar Momento Atual com dados do sprint
       const momento: MomentoAtualEntry = {
         velocity: `${sprintEntry.spEntregues}/${sprintEntry.spPlanejados} SP`,
         cycleTime: `${sprintEntry.cycleTimeMedio}d`,
         codeReview: `${github?.avgCommentsPerReview?.toFixed(1) ?? '0'} comments/PR`,
         alertasAtivos: bloqueios.length,
       }
       metricsWriter.updateMomentoAtual(person.slug, momento)
     } catch (err) {
       log.warn('falha ao persistir metricas de sprint', { slug: person.slug, error: err instanceof Error ? err.message : String(err) })
     }
   }
   ```

3. NAO persistir analise IA no metricas.md (per D-05).
4. try/catch garante que falha na persistencia nao impede geracao do relatorio.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit src/main/external/SprintReportGenerator.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - SprintReportGenerator.ts contains `import { MetricsWriter` from `'./MetricsWriter'`
    - SprintReportGenerator.ts contains `metricsWriter.writeSprint(`
    - SprintReportGenerator.ts contains `metricsWriter.updateMomentoAtual(`
    - SprintReportGenerator.ts contains try/catch around metricsWriter calls
    - SprintReportGenerator.ts contains `sprint.name` being passed to SprintEntry.nome
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>SprintReportGenerator persiste resultado consolidado (SP entregues/planejados, cycle time, bloqueios) no metricas.md e atualiza Momento Atual. Graceful degradation via try/catch.</done>
</task>

<task type="auto">
  <name>Task 2: Integrar MetricsWriter no MonthlyReportGenerator</name>
  <files>src/main/external/MonthlyReportGenerator.ts</files>
  <read_first>
    - src/main/external/MetricsWriter.ts (verificar exports MonthlyEntry, MomentoAtualEntry e assinaturas)
    - src/main/external/MonthlyReportGenerator.ts (entender generate() flow completo, PersonMonthlyData, baseline)
    - .planning/phases/999.7-perfil-vivo-sensibilizado-por-daily-report/999.7-CONTEXT.md (decisoes D-04, D-05, D-09, D-12)
  </read_first>
  <action>
**MonthlyReportGenerator.ts** (per D-04, D-09, D-12):

1. Adicionar import no topo:
   ```typescript
   import { MetricsWriter, type MonthlyEntry, type MomentoAtualEntry } from './MetricsWriter'
   ```

2. No metodo `generate()`, APOS a linha `writeFileSync(filePath, content, 'utf-8')` (linha ~126) e ANTES do `return filePath`:
   ```typescript
   // Persistir metricas no metricas.md (per D-04, D-12)
   const metricsWriter = new MetricsWriter(this.workspacePath)
   for (const person of personReports) {
     try {
       const jira = person.snapshot?.jira
       const github = person.snapshot?.github

       // Calcular deltas vs mes anterior (per D-09)
       const deltas: Record<string, string> = {}
       if (person.previous.commits30d !== undefined) {
         const diff = person.monthlyGithub.commits - person.previous.commits30d
         deltas['commits'] = `${person.monthlyGithub.commits} (${diff >= 0 ? '+' : ''}${diff} vs mês anterior)`
       } else {
         deltas['commits'] = `${person.monthlyGithub.commits} (sem comparação)`
       }
       if (person.previous.prsMerged30d !== undefined) {
         const diff = person.monthlyGithub.prsMerged - person.previous.prsMerged30d
         deltas['PRs merged'] = `${person.monthlyGithub.prsMerged} (${diff >= 0 ? '+' : ''}${diff} vs mês anterior)`
       } else {
         deltas['PRs merged'] = `${person.monthlyGithub.prsMerged} (sem comparação)`
       }
       deltas['reviews'] = `${person.monthlyGithub.reviews}`
       deltas['cycle time'] = `${jira?.tempoMedioCicloDias ?? 0}d`
       deltas['collaboration'] = `${github?.collaborationScore ?? 0}`

       // Destaques e pontos de atencao
       const destaques: string[] = []
       const pontosAtencao: string[] = []

       // Usar workloadScore como indicador
       if (jira?.workloadScore === 'alto') {
         pontosAtencao.push('Workload alto no período')
       }
       if (jira?.blockersAtivos && jira.blockersAtivos.length > 0) {
         pontosAtencao.push(`${jira.blockersAtivos.length} blocker(s) ativo(s)`)
       }
       if (person.monthlyGithub.prsMerged > 0) {
         destaques.push(`${person.monthlyGithub.prsMerged} PRs merged`)
       }
       if (person.monthlyGithub.reviews > 0) {
         destaques.push(`${person.monthlyGithub.reviews} code reviews`)
       }

       const monthlyEntry: MonthlyEntry = {
         mes: `${MESES[targetMonth - 1]} ${targetYear}`,
         destaques,
         pontosAtencao,
         deltasVsMesAnterior: deltas,
       }
       metricsWriter.writeMonthly(person.slug, monthlyEntry)

       // Atualizar Momento Atual com dados mensais
       const velocityTrend = person.baseline
         ? (person.monthlyGithub.prsMerged > person.baseline.avgPRsMerged ? '↑' : person.monthlyGithub.prsMerged < person.baseline.avgPRsMerged ? '↓' : '↔')
         : '↔'
       const momento: MomentoAtualEntry = {
         velocity: `${jira?.storyPointsSprint ?? 0} SP ${velocityTrend}`,
         cycleTime: `${jira?.tempoMedioCicloDias ?? 0}d`,
         codeReview: `${person.monthlyGithub.reviews} reviews, ${github?.avgCommentsPerReview?.toFixed(1) ?? '0'} comments/PR`,
         alertasAtivos: jira?.blockersAtivos?.length ?? 0,
       }
       metricsWriter.updateMomentoAtual(person.slug, momento)
     } catch (err) {
       log.warn('falha ao persistir metricas mensais', { slug: person.slug, error: err instanceof Error ? err.message : String(err) })
     }
   }
   ```

3. As variaveis `targetYear`, `targetMonth`, `MESES` ja existem no escopo do generate().
4. try/catch garante que falha na persistencia nao impede geracao do relatorio.
  </action>
  <verify>
    <automated>cd /Users/guilhermeaugusto/Documents/workspace-projects/pulse-cockpit && npx tsc --noEmit src/main/external/MonthlyReportGenerator.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - MonthlyReportGenerator.ts contains `import { MetricsWriter` from `'./MetricsWriter'`
    - MonthlyReportGenerator.ts contains `metricsWriter.writeMonthly(`
    - MonthlyReportGenerator.ts contains `metricsWriter.updateMomentoAtual(`
    - MonthlyReportGenerator.ts contains try/catch around metricsWriter calls
    - MonthlyReportGenerator.ts contains `MESES[targetMonth - 1]` for month name
    - MonthlyReportGenerator.ts contains `deltasVsMesAnterior` with computed deltas
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>MonthlyReportGenerator persiste tendencia mensal (destaques, pontos de atencao, deltas vs mes anterior) no metricas.md e atualiza Momento Atual. Graceful degradation via try/catch.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` sem erros nos 2 arquivos (SprintReportGenerator, MonthlyReportGenerator)
2. SprintReportGenerator chama writeSprint e updateMomentoAtual com dados do PersonSprintData
3. MonthlyReportGenerator chama writeMonthly e updateMomentoAtual com dados do PersonMonthlyData e deltas calculados
4. Nenhuma analise IA persiste no metricas.md (per D-05)
5. Ambos usam try/catch para graceful degradation
</verification>

<success_criteria>
- SprintReportGenerator persiste resultado de sprint no metricas.md como ultimo passo
- MonthlyReportGenerator persiste tendencia mensal com deltas no metricas.md como ultimo passo
- Ambos atualizam secao Momento Atual
- Compilacao TypeScript passa sem erros
- Graceful degradation: falha na persistencia nao impede geracao de relatorio
</success_criteria>

<output>
After completion, create `.planning/phases/999.7-perfil-vivo-sensibilizado-por-daily-report/999.7-02-SUMMARY.md`
</output>
